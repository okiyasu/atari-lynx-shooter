# APS-018 / v004

- 作成日時: 2026-08-04T14:10:00Z
- 前版: `v003.md`
- 発行理由: v003でBGM/速度/HUD表記の主要矛盾は解消したが、Dev Frontの最終照合で`docs/plan/design.md`に実装値と異なるHUD幅、および旧更新経路の表現が残ったため。
- 前版成果の要約: 実装、GUI証跡、`./scripts/verify.sh`（ゲーム523件・サウンド127件）、`make smoke-host`（7件）、ASan/UBSan、LNX（36,933 bytes、SHA-256 `2fb2ac6f4b16173e29eb01e7fef972c1b44dec973f9581d115cddc0292c0d8e0`）は完了。v003では文書のみを更新した。

## 最初に読むもの

対象プロジェクトは`/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`です。存在する`AGENTS.md` / `CLAUDE.md`（現時点は`CLAUDE.md`）、`ISSUES.md`、`issues/`の該当詳細（あれば）、`docs/plan/design.md`、v003を読んでから着手してください。作業後は必ず`ISSUES.md`を更新してください。

## 是正内容（文書のみ）

- `docs/plan/design.md`のAPS-018 HUD説明にある「80px以内」を、実装の3x5文字・21文字・4pxピッチ（開始X=2、最大X=84、表示幅83px）と矛盾しない表現へ正す。画面160px幅の一行である点は維持する。
- 同設計書のMIKEYバックエンド説明にある「`game_update()`後」を、現行Lynxアダプタの実装どおり「その描画フレームの`game_update_logic()`群と75Hzの`game_sound_tick()`後」等へ正す。`game_update()`はホスト互換ラッパーであり、Lynxのmain loopが直接呼ばないことを誤解させない。
- `ISSUES.md`へこのv004是正と再検証を追記して状態を実装・検証完了へ戻す。

## 保全・禁止範囲

変更可能なのは`docs/plan/design.md`と`ISSUES.md`だけ。`src/`、`include/`、`tests/`、`README.md`、GUI証跡、発行済みブリーフ、ツールチェーン、Git設定、BIOS/外部ROM/外部素材は変更禁止。reset、checkout、stash、削除、コミット、push、deployも禁止。

## 完了条件

- 上記2表現を実装と照合して是正し、`80px以内`とLynx main loopの`game_update()`直接呼出しを文書から除去する。
- `./scripts/verify.sh`、`make smoke-host`、`sh -n scripts/*.sh`、`git diff --check`、LNXヘッダ検査を実行し、終了コード・件数・ROMサイズ・SHA-256を`ISSUES.md`へ記録する。v002のASan/UBSan実測がコード非変更により引き続き有効なことも記録する。
- 最終報告は変更ファイル、是正内容、実測、ROMハッシュ、残課題を簡潔に記す。
