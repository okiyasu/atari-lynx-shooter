# APS-018 / v005

- 作成日時: 2026-08-04T14:14:00Z
- 前版: `v004.md`
- 発行理由: v004のHUD文字数計算が誤っていた。Devのソース照合を正とし、正しい実装値へ訂正して文書是正を完了する。
- 前版からの変更: HUDは**20文字**（`hud_text[0]`〜`[19]`、`[20]`はNUL）、3x5文字・4pxピッチ・開始X=2、最終文字の右端X=80、表示幅79pxである。21文字／X=84／83pxというv004の値は使用禁止。

## 最初に読むもの

対象プロジェクトは`/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`です。存在する`AGENTS.md` / `CLAUDE.md`（現時点は`CLAUDE.md`）、`ISSUES.md`、`issues/`の該当詳細（あれば）、`docs/plan/design.md`、v003・v004を読んでから着手してください。作業後は必ず`ISSUES.md`を更新してください。

## 文書是正内容

- `docs/plan/design.md`のAPS-018 HUD説明の幅を、実装どおり「20文字、開始X=2、右端X=80、表示幅79px（160px画面の一行）」へ更新する。`80px以内`という既存文言を残してもよいが、上記の厳密値と矛盾しないこと。
- 同設計書のMIKEYバックエンド説明にある「`game_update()`後」を、現行Lynx main loopの実装どおり「その描画フレームの`game_update_logic()`群と75Hzの`game_sound_tick()`後」等へ更新する。`game_update()`はホスト互換ラッパーであり、Lynx main loopが直接呼ばない。
- `ISSUES.md`にv004は未実施（ブリーフ前提の誤りをDevが検出）であること、v005で正しい文書是正を完了したこと、再検証結果を追記し、状態を実装・検証完了へ戻す。

## 保全・禁止範囲

変更可能なのは`docs/plan/design.md`と`ISSUES.md`だけ。`src/`、`include/`、`tests/`、`README.md`、GUI証跡、発行済みブリーフ、ツールチェーン、Git設定、BIOS/外部ROM/外部素材は変更禁止。reset、checkout、stash、削除、コミット、push、deployも禁止。

## 完了条件

- 正しいHUD実測、Lynx更新経路、BGM停止/1.25倍/SFX 75Hzの現行文書を相互に照合する。
- `./scripts/verify.sh`、`make smoke-host`、`sh -n scripts/*.sh`、`git diff --check`、LNXヘッダ検査を実行し、終了コード・件数・ROMサイズ・SHA-256を`ISSUES.md`へ記録する。コード非変更のためASan/UBSanはv002実測を有効として記録する。
- 最終報告には、v004の前提誤りを実装値に訂正したこと、変更ファイル、実測、ROMハッシュ、残課題を簡潔に記す。
