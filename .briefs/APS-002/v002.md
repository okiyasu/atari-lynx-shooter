# APS-002 Dev差し戻しブリーフ

- 課題ID: APS-002
- 版番号: v002
- 作成日時: 2026-08-02T23:37:37+09:00
- 前版: `.briefs/APS-002/v001.md`
- 対象プロジェクト: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 差し戻し理由

v001に基づく実装と`./scripts/verify.sh`は成功し、`PASS: 35 game logic checks`、LNX 8,384 bytes、SHA-256 `5683b479c9ac842621c00b47dc2d4b1fb02f729263f2e83fc244eee825907a53`をDev Frontでも再現した。実装に明白な仕様違反はない。

ただし、「APS-002の確定仕様をすべてホストテストで自動検証する」という完了条件に対し、次の2点が明示的にassertされていない。

1. 非最終の残機喪失後も得点と自機X/Y座標が維持されること。
2. GAME OVER中に3発すべての弾状態が凍結されること。現行テストは`bullets[0].active`のみ比較している。

## 作業前の必須確認

- プロジェクト直下の`CLAUDE.md`と`ISSUES.md`を先に読むこと。
- `docs/plan/design.md`と`.briefs/APS-002/v001.md`も読み、確定仕様と既存実装の全体像を確認すること。
- 作業後は`ISSUES.md`のAPS-002項を更新すること。

## 修正要件

- `tests/test_game.c`に、非最終の残機喪失直前に0以外の得点と初期値以外の自機X/Yを設定し、被弾後にそれらが不変であることを明示assertする。
- `tests/test_game.c`に、GAME OVER状態の全3発に区別可能な`active`または座標状態を設定し、移動・発射入力付きで複数フレーム更新しても、全3発の状態が不変であることを明示assertする。
- 既存の35チェック、特にGAME OVER成立時からA/Bを押し続けても再開始せず、解除後の再押下でのみ再開始する回帰テストを維持する。
- 追加テストが現行実装の不具合を示した場合のみ、APS-002の確定仕様の範囲で共有ロジックを修正する。

## 変更してよい範囲

- `tests/test_game.c`
- `ISSUES.md`のAPS-002項（再検証結果、チェック件数、差し戻し対応の追記）
- 追加テストで不具合が再現した場合のみ`include/game.h`と`src/game.c`

## 触ってはいけない範囲

- `.briefs/APS-002/v001.md`と本`v002.md`を含む発行済みブリーフ
- `src/main.c`、`README.md`、`docs/plan/design.md`、`CLAUDE.md`、`docs/plan/toolchain-research.md`、`Makefile`、`scripts/`、`.gitignore`
- `.cache/`内の第三者依存物、本課題と無関係な機能、ツールチェーン、フレーム同期方式
- `.env`、秘密鍵、BIOS、`lynxboot.img`、外部ROM、外部素材。読まず、探索せず、取得・生成・同梱しないこと。

## 完了条件

1. 上記2点を明示的に自動検証するホストテストが追加され、既存35チェックを含む全テストが成功する。
2. `./scripts/verify.sh`を実行し、クリーンROMビルド、clang厳格C89 warnings-as-errors、cc65警告エラー化、shell lint、LNXヘッダ検査がすべて成功する。実際のチェック件数と出力を報告する。
3. `git diff --check`が成功し、`git status --short`と`git diff --stat`が想定外の変更を示さない。
4. ROMサイズとSHA-256を再計測する。共有ロジックが不変でROMも不変な場合はその旨を報告する。
5. `ISSUES.md`のAPS-002状態を「実装・自動検証完了」へ戻し、v002の追加テスト内容と再検証実測値を追記する。
6. 設計からの差分の有無と未確認事項を再報告する。
7. コミット・push・BIOSや外部ROMの操作は行わない。

完了報告は、変更内容、実測テスト結果、設計差分、未確認事項の順で簡潔にまとめる。
