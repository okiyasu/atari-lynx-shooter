# APS-035 / v001

- 作成日時: 2026-08-09 JST
- 前版: なし（新規課題）
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 事前確認

`AGENTS.md` / `CLAUDE.md` のうち存在するもの、`ISSUES.md`、`issues/`配下の該当詳細、`docs/plan/`の設計書、APS-033/034のbriefと現行成果を先に読むこと。作業後は`ISSUES.md`を更新すること。基点は未コミットAPS-031〜034（現行version `0.35.0`）であり、既存差分を保全する。

## 要求

ユーザー確認: V0.34.0のタイトル音声は早すぎた。直前の15,873.016 Hz（Timer 3 backup 62、63 us）の2倍速を取り消し、**再生レートだけ**を半分、約7.94 kHzへ戻す。

- `src/title_voice_stream.s`のTimer 3 reloadを`125`へ戻す。1 us clockでは周期は`backup + 1`なので、126 us/sample、7,936.508 Hzである。15,873.016 Hzの正確な半分で、V0.33相当。
- 17,555 samplesの計算再生時間は約2.211930秒。8 kHz生成済みdataを7,936.508 Hzで消費するため、V0.34の高ピッチ化を取り消す（厳密な8 kHzではないがV0.33と同じ設定）。
- 音声asset、ADPCM内容、データ生成/変換/TTS、cart配置を一切変更しない。`assets/voice/title-start.adpcm`のSHA-256 `2c8e8402f6b059de5e746b7513be97626f3301a0aba6f2644da62b82d5b30c6a`を不変として検査する。
- 16 kHzで導入済みの専用IRQ、exact IMA decoder、128-byte x5 buffer、3段queue、連続pump/title gateを単に削除・巻戻ししない。rate変更だけで動作させ、必要ならコメントを現在速度へ正す。BGM/SFX/A-C/B/D channel配線、Timer 0/2/7、75Hz描画・入力/SFX、300Hz logic、APS-034のstage JSON/generator/sprite/paletteに触れない。
- タイトルのarmed→FIRE→一回だけ音声開始→再生中入力無視→実際の再生完了後に一回だけ`game_start()`、Timer 3/channel D停止、channel A BGM開始を保全する。

## 変更可能範囲

`src/title_voice_stream.s`、`include/title_voice_stream.h`、`scripts/verify-title-voice-gearlynx.py`、voice playbackの期待値を持つ既存test、`include/version.h`、`README.md`、`docs/plan/design.md`、`ISSUES.md`、`.briefs/APS-035/`、検証で生成されるGit管理外`build/`/`dist/`/`.cache/`のみ。必要最小限のMakefile検証調整は可。

変更禁止: 音声asset・metadata・生成script・codec・cart directory、BGM/SFX、ゲームplay state、stage authoring/generator/generated table/sprite/palette、既存briefの上書き、外部依存/素材/BIOS、commit/push/stash/reset/checkout。

## 検証・記録

1. `make clean && ./scripts/verify.sh`、`make perf-host`、`git diff --check`を実行して結果を報告する。strict C89/warnings-as-errors、host/game/sound/IMA/stage/sprite回帰、asset SHA、LNXを通すこと。
2. 最終LNXで`python3 scripts/verify-title-voice-gearlynx.py`を実行し、Timer 3 `backup=0x7D`、control `0xD8`、period 126 us、7,936.508 Hz、channel D DAC列のC89 IMA reference一致、17,555 IRQ/sample、`underrun=0`、停止後zero、完了一回だけのタイトル→game開始、channel A BGM開始を確認する。実機相当のGearlynx結果と実機未確認を区別して記録する。
3. `GAME_VERSION_STRING`を`0.36.0`へ更新する。V0.35.0はAPS-034 ROM生成済みなので、ROM単位の単調増加を守る。
4. README/design/ISSUESへ、asset不変・7,936.508 Hz・約2.212秒・高ピッチの16 kHz化を取り消した事実、実機負荷/音質未確認を記録する。APS-033の履歴記録は改竄せず、APS-035として現行仕様を追加する。

完了報告は変更、実行コマンドと結果、ROM size/SHA-256、Gearlynx実測、設計差分、残リスクを構造化して返すこと。
