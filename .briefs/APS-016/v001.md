# APS-016 Dev委譲ブリーフ

- 課題ID: APS-016
- 版番号: v001
- 作成日時: 2026-08-04T10:18:20+09:00
- 前版: なし（初版）
- 発行理由: 起動時タイトルと、GAME OVERからタイトルへ安全に戻る入力導線を追加する。

対象プロジェクト絶対パス: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

着手前に、存在する`AGENTS.md` / `CLAUDE.md`のうち存在するもの（現時点では`CLAUDE.md`のみ）と`ISSUES.md`を必ず先に読むこと。続けて`docs/plan/design.md`、`README.md`、`Makefile`、`src/game.c`、`src/main.c`、`include/game.h`、既存テストを読むこと。`issues/`は現時点で不在。作業後は必ず`ISSUES.md`のAPS-016を更新すること。

同一作業ツリーはAPS-014/015の未コミット成果を含むが、実行中の他エージェントはなく競合なしを確認済み。既存差分をreset、checkout、stash、改変、削除しないこと。特にAPS-014の静的BSS移動・完全初期化、APS-015のスモークとGearlynx診断を保全する。コミット、push、deploy、BIOS・`lynxboot.img`・外部ROM/素材の取得・探索・読取・複製・生成・同梱は禁止。

## 実装仕様

固定状態機械へタイトル状態を追加する。起動時はタイトルで、`ASTEROID PATROL`、`A/B TO START`、方向移動・A/B射撃の基本操作を160x102内に読みやすく表示する。タイトルでのA/B押下は即時に新規ゲームを開始し、Stage 1 `GAME_PHASE_STAGE_INTRO`、スコア0、残機3、武器Lv1、クリーンな戦闘物、Stage 1曲頭となる。

GAME OVERは最終フレームのゲーム背景（背景・HUD・残存戦闘物を必要な範囲で凍結表示）を保持し、`GAME OVER`と`A/B TO TITLE`だけを重ねる。GAME OVERへ入った押下を使わず、A/Bを一度離してから再押下した時だけタイトルへ戻す。タイトル復帰は音を停止し、タイトルから次に開始する新規ゲームを完全初期化する。既存ALL CLEARのA/Bを離して再押下→Stage 1完全再開始は変更しない。

`GameState`の明示的なタイトル/入力アーム状態、ゲーム開始/タイトル復帰の初期化境界を設計し、ゲームロジックをホストテスト可能に保つ。TGIの`busy`待機→入力→`game_update`→サウンド反映→描画→`tgi_updatedisplay()`の順、MIKEYの許可済み書込み範囲、静的BSSの`game`（TGI第2バッファ`0xC038`と分離）を保持する。タイトル中・GAME OVER中に敵/弾/状態を意図せず進めない。通常`make verify`にGearlynxを必須化しない。

変更可: `include/game.h`、`src/game.c`、`src/main.c`、`tests/test_game.c`、必要なら`tests/test_smoke.c`、`README.md`、`docs/plan/design.md`、`ISSUES.md`、`.briefs/APS-016/v001.md`。変更禁止: `CLAUDE.md`、`.gitignore`、`scripts/`、リンカ設定、過去の`.briefs/`、APS-014/015の既存実装を無関係に変更すること、BIOS/外部ROM/素材、コミット、push、deploy、stash、reset、checkout。

## 完了条件

- 起動→タイトル、A/B→Stage 1導入、開始時の初期値、GAME OVERの離す→再押下→タイトル、タイトルからの再開始、ALL CLEARの従来再開始をホスト回帰で追加・実行する。
- `make clean && ./scripts/verify.sh`、ASan/UBSan付きゲーム・サウンド・追加スモーク、`sh -n scripts/*.sh`、LNX検査、`git diff --check`を実行し、各終了コード、件数、ROMサイズ/SHA-256を提示する。
- `ISSUES.md`へ変更内容、設計差分、実測、未確認事項、禁止操作不実施を記録する。報告は変更ファイル、テスト結果、入力/音/描画/進行の整合、残るリスクを簡潔にまとめる。コミット・pushはしない。
