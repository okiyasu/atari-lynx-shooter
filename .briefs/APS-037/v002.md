# APS-037 / v002

- 作成日時: 2026-08-09T11:01:05+09:00
- 前版: `v001.md`
- 種別: 再生成後のstrict verify不整合是正

## 対象・着手前確認

`/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

存在する `AGENTS.md` / `CLAUDE.md`、`ISSUES.md`、`docs/plan/`、`assets/voice/`、`v001.md` と本ブリーフを先に読むこと。作業後は `ISSUES.md` を更新すること。

## 前回成果と実測不整合

VOICEVOX Nemo 0.24.0 arm64をlocalhost `127.0.0.1:50121`で実起動し、両assetを再生成した。

- title: ADPCM `99eb68abe7da548a7285510c86dec9417e94766d00ac30638de302a2cd6a1eb2`、17,408 samples、8,704 bytesは一致。一方raw WAV/正規化PCM hashは初回記録値から変化した。
- game-over: ADPCM `848691fea26de6e2503c67bed5721f1da27cab1692af81e2227a348ab412cb0f`、11,691 samples、5,846 bytesは一致。
- この後 `make voice-check` はtitleの `pcm, wav` mismatchでFAILした。現在のscriptはraw WAV/PCMを固定hashとして要求するが、VOICEVOX生成出力は最終ADPCMが同一でもraw PCM hashが常に同一とは保証されない実測。

これは前版の「再生成可能なstrict verify」を満たしていない。事実を隠すためにraw hashを固定値へ戻したり、metadataへ実際と異なるhashを書いたりしないこと。

## 変更範囲

- `scripts/generate-title-voice.py`、`assets/voice/*.json`、`assets/voice/README.md`、`assets/voice/LICENSE.md`、`README.md`、`ISSUES.md`、`docs/plan/design.md`、必要な回帰テストのみ。
- raw WAV/PCMのhashは生成run provenanceとしてmetadataに保持してよい。ただしoffline verifyはその値の形式・WAV format・query hash・sample count・PCM正規化条件・最終ADPCM hash・header/cart整合を検査し、raw WAV/PCM hashのrun間完全一致を再現性条件にしないこと。
- 可能ならraw PCMが変わっても最終IMA ADPCM・sample count・headerが同一となることをhost回帰に明文化する。raw outputの非決定性が検証不能または最終ADPCMまで不安定なら停止して報告すること。

## 変更禁止

- v001の選定（VOICEVOX Nemo 男性2、credit `VOICEVOX:Nemo（男性2）`）、asset文言、8 kHz IMA ADPCM仕様、Timer 3/channel D/shared PCM/state machine、BGM/SFX gain、stage/sprite/ゲーム進行。
- 外部API・外部送信・Personal Voice・第三者素材、commit / push / stash / reset / checkout。

## 完了条件

1. ローカルNemo 0.24.0 arm64を起動した状態で `make voice-generate`、`make voice-generate-game-over`、`make voice-check` をこの順に実行してPASS。両最終ADPCMのhash/sample count/headerが上記値と一致。
2. raw WAV/PCM hashがrun provenanceでありcross-run reproducibility保証ではない理由と、最終ROM同梱物の決定性境界をREADME/design/ISSUESへ正直に記録。
3. `make clean && ./scripts/verify.sh`、`git diff --check`、LNX/cart検査PASS。0.38.0 ROMの再ビルドhashを報告。
4. v001要求を保全し、設計差分・残る実機未確認を報告。
