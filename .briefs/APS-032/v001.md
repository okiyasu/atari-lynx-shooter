# APS-032 / v001

- 作成日時: 2026-08-08 JST
- 前版: なし
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 依頼

`AGENTS.md` / `CLAUDE.md`の存在するもの、`ISSUES.md`、`docs/plan/`、APS-031実装を先に読むこと。作業後は`ISSUES.md`を更新すること。基点はAPS-031を含む未コミットV0.32.0。既存差分を上書きせず、commit/push/stash/reset/checkout禁止。

ユーザー明示許可: macOSローカル`/usr/bin/say`（ネットワーク/APIなし）で「わしは宇宙の帝王ザカリテ」を生成し、タイトル開始時に再生する。インストール済み日本語voice（Kyoko等）だけを使い、voice名・OS依存・生成日時・入力文言を記録する。再生成可能な`assets/voice/`のテキスト入力と生成/変換スクリプトを追加し、最終8kHz mono IMA ADPCMデータはGit管理する。原音AIFF/WAVなどの生成物は同梱しない。外部素材・商用TTS・Personal Voice・APIキーは使用/取得しない。Apple macOSの利用規約に再配布出力の明示許諾を発見できない場合、その事実と採用者の確認要をライセンス注記へ記録する（断定しない）。

APS-031のchannel D/Timer 3 PCM streamを実音声向けに完成させる。4-bit IMA ADPCMを8kHzで復号してDACへ送る実装を追加し、割込み内のC復号を安易に入れない。assemblyまたは事前デコードring buffer等、cc65 65SC02で連続供給できる設計を選び、欠落/終端/length 0/stop/再生中start無視をテストする。音声長・ADPCM byte数・ROM/RAM差・cycle/割込周期見積を実測/記録する。現行resident余地は約6,262 bytesであり、自然長8kHz IMA ADPCMが不適合なら短縮・PCM・sample rate変更を勝手に採用せず、統合を止めて比較案と実測を報告する。

容量/性能が適合した場合のみ、タイトルの既存`title_start_armed`後の最初のFIREでPCM開始し、即`game_start()`しない。再生中FIREは無視し、75Hz描画/入力1回・300Hzロジック・BGM4倍/SFX75Hzを維持。PCM完了を一度だけ観測して`game_start()`、全ゲーム経路（GAME OVER/ALL CLEAR/再初期化を含む）でPCM停止。A/C=BGM、B=SFX、D=タイトルPCMの排他を守る。ゲームオーバー音声「お前は弱かった」は実装しない。

変更可: APS-031 codec/PCM backend、タイトル遷移に必要な`game.*`/`main.c`、独立voice assets/scripts/tests/Makefile/docs/ISSUES/.briefs。変更禁止: 既存BGM/SFXテーブル/MML/低音ベース/ゲーム速度・入力頻度、外部依存/素材。`include/version.h`を`0.33.0`へ。

完了条件: C89/warnings-as-errors、codec・game/sound回帰、`make clean && ./scripts/verify.sh`、ASan/UBSan、`make perf-host`、LNX検査、SHA-256、Gearlynxでchannel DのDAC変化と完了後停止（可能ならタイトル遷移）を検証。実行コマンド/結果、設計差分、実機未確認事項を構造化報告すること。
