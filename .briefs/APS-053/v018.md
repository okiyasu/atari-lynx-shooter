# APS-053 v018 — 公開symbol限定の表示境界診断

- 作成日時: 2026-08-12T21:50:00+09:00
- 前版: `v017.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 前回成果: v015/v016でROM無変更のsection/logic診断を完了。Fable5 v017は、Timer2局所単価だけではframe逆転を説明できず、公開symbolによる表示境界分離を次単位と決定した。

## 着手前に読むもの

存在する`AGENTS.md` / `CLAUDE.md`と`ISSUES.md`を先に読むこと（本プロジェクトは`CLAUDE.md`のみ）。さらに`docs/plan/2026-08-12-suzy-sprite-migration-v2.md`、`.briefs/APS-053/v014.md`〜`v017.md`、`evidence/APS-053/{section-profile-v015.json,logic-profile-v016.json,README.md}`、`src/main.c`、`src/static_layer.c`、cadence verifier群、cadence `.lbl/.map`を読むこと。作業後は`ISSUES.md`を更新すること。

## 目的

ROM無変更・公開symbol限定のverifierで、0敵/4敵/boss fixtureの表示境界を分離する。修理、スケジューラ変更、閾値変更、Phase 3R、ROM内プロファイラは実施しない。

## 変更してよい範囲

- cadence Gearlynx verifier新設または既存拡張、Makefile診断専用ターゲット`phase-2r-display-profile-gearlynx`、`evidence/APS-053/`、同README、`ISSUES.md`。

## 触ってはいけない範囲

- release/cadence ROMのC/asm、`src/`、`include/`、`cfg/`、C stack予約、背景データ、Suzy collision、Phase 3R、音声/可動object/collision/logic/input/Timer/IRQ。
- ROM内プロファイラ、内部関数addressの推測、`evidence/APS-052/logic-catchup-gearlynx.json`上書き、コミット・push・stash・reset・checkout禁止。

## 実装要求

1. cadence `.lbl`で次の公開symbolを解決してから使用する: `_game_timing_consume_vblanks`、`_game_update_logic`、`_game_sound_tick`、`_static_layer_draw`、`_tgi_ioctl`。未解決なら根拠付きFAILで停止。
2. `_tgi_ioctl`のfastcall引数ABIを、既存ソース/cc65 ABI/既存O2証跡で確認する。推測でcode/dataを分類しない。安全に確認できた場合だけ code `0`=`tgi_sprite`、code `4` data `0`=`tgi_busy`、code `4` data `1`=`tgi_updatedisplay`を分類する。確認不能なら分類なしで境界時刻だけ保存してFAIL。
3. 0敵NORMAL、4敵NORMAL、4敵+BOSS BOSSを各10表示間隔・独立2 batchで測定。各intervalで以下を保存する。
   - consume時のelapsed VBlank、logic/sound更新数、Timer2
   - `_static_layer_draw`到達時刻
   - `tgi_sprite`件数
   - `tgi_busy`の発生と到達時刻
   - `tgi_updatedisplay`到達時刻
   - fixture live readback、ROM SHA、map/MAIN余剰
4. 0敵fixtureでprofile/no-profile陰性対照を行う。絶対中央値差≤1 VBlankかつ相対差≤5%以外は`debugger_timing_contamination` FAILとして停止する。
5. `evidence/APS-053/display-profile-v018.json`へraw/集計/境界分類の根拠/分岐結果を保存。ROM SHA・map Segment list（CODE/RODATA/DATA/BSS）・MAIN余剰が不変であることをassertする。表示/描画側が60%以上でも最適化を開始しない。
6. Fable5 v017の設計差分（hsize/vsize主因の撤回候補、ROM内プロファイラはユーザー承認必須、修理ゲートBLOCKED）をREADME/ISSUESへ記録する。v2設計書本文は変更しない。

## 完了条件

- `make phase-2r-display-profile-gearlynx`がPASS、または安全なABI分類不可/汚染の根拠付きFAIL。
- `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、`make debug-contract`、`make static-layer-readback-gearlynx`、対象`py_compile`、`git diff --check`を実行すること。
- 実行結果、map CODE/RODATA/DATA/BSS/MAIN余剰、未解決事項、設計との差分をISSUESと報告へ記載すること。
