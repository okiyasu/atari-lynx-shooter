# APS-053 v005 — 2R-0/2R-1検収不足の補完

- 課題ID: APS-053
- 版: v005
- 作成日時: 2026-08-12 JST
- 前版: `v004.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 着手前必須

`AGENTS.md`（存在すれば）、`CLAUDE.md`、`ISSUES.md`、`docs/plan/2026-08-12-suzy-sprite-migration-v2.md`、`.briefs/APS-053/v004.md`、`evidence/APS-053/phase-2r-v004.json`を先に読むこと。作業後`ISSUES.md`を更新すること。コミット・push・stash・reset・checkoutは禁止。

## v004検収結果

基準C stack予約を保持した通常/cadenceリンク、MAIN spare 444/406 bytes、0x0100通常SCB scale、penpal全8 byte初期化、runtimeの`tgi_outtextxy`除去は確認済み。だがv004必須だった「voice共有のdebug assert」とリンク回復後の実Gearlynx音声回帰（title/GAME OVER、MIKEY channel 0/1/2）は完了報告・証跡がない。ここだけを補完し、Phase 2R-2 cadence/pixel/readbackやPhase 3Rへ進まないこと。

## 変更してよい範囲

`src/static_layer.c`、`include/static_layer.h`、必要最小限のテスト/証跡/ISSUES/README/design文書のみ。可動object、sprite data pipeline、collision、Timer/IRQ/audio実装・cart、C stack予約、背景データ、2R-1のCODE構成を変更しない。BSS増加禁止。MAIN spareが通常/cadenceとも256B未満になれば停止する。

## 必須

1. scratch共有契約をdebug buildで検出可能にする。voice再生中に`static_layer_draw()`、`static_layer_text()`、title/credit APIが呼ばれた場合、既存release runtime guardに加えて明示的なdebug assertion/fail-fastとなること。headerに対象範囲(先頭539B)・voice所有権・非同時利用を記す。assertがrelease ROMへ残らない/最小化される場合も、debugビルドで有効化する手順と検出結果を証跡へ記録する。
2. 正常経路ではtitle/GAME OVERのvoice中に静的描画APIを呼ばないことをGearlynxの実ROM回帰で確認する。`python3 scripts/verify-title-voice-gearlynx.py --mode title`、`python3 scripts/verify-title-voice-gearlynx.py --mode game-over`を実行する。
3. `python3 scripts/verify-audio-gearlynx.py --channel 0 --seconds 8`、`--channel 1 --seconds 8`、`--channel 2 --seconds 20`を実行し、pitch変化と75% gain回帰を確認する。
4. `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、関連`py_compile`、`git diff --check`を再実行し、通常/cadence mapのCODE/RODATA/DATA/BSS/STARTUP合計とMAIN spareを更新する。

## 完了条件

- 基準CFG不変、通常/cadence MAIN spare各256B以上、BSS増加なし。
- debug assertionの有効化・意図的違反検出と、release正常経路のvoice/API非競合を証跡化。
- 上記全音声回帰と全ビルド/host検証PASS。`evidence/APS-053/`、ISSUES/README/designを更新し、versionは挙動変更時のみ更新する。
- 未達時は成功扱いにせず、最後のコマンド結果・map差分・ブロッカーを報告。commit/pushしない。
