# APS-053 v022 — Phase 1 catch-up 因果分離診断

- 作成日時: 2026-08-12T23:58:00+09:00
- 前版: `v021.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 前回成果: v021は4敵初回注入後、再注入なしで`153/154`級から中央値`31` VBlankへ減衰し、enemy slot 12・enemy bullet slot 3の状態進化を確認。Fable5再設計では、現行`elapsed×4`・logic最大128/sound最大2048の一括catch-upが遅延を増幅し得ると判断。ただしcatch-up主因は未確定。

## 着手前に読むもの

存在する`AGENTS.md` / `CLAUDE.md`と`ISSUES.md`を先に読むこと（本プロジェクトは`CLAUDE.md`のみ）。さらに`.briefs/APS-053/v019.md`、`v021.md`、`docs/plan/2026-08-12-suzy-sprite-migration-v2.md`、`evidence/APS-053/{display-profile-v019.json,display-profile-v021.json,README.md}`、`scripts/verify-display-profile-gearlynx.py`、`src/main.c:799-825`、`include/game.h:49-57`、`src/cadence_probe.s`を読むこと。作業後は`ISSUES.md`を更新すること。

## 目的

ROM動作コードを変えず、4敵NORMAL fresh（毎interval再注入）とno-reinject（初回だけ注入）を各2 batch×10 intervalで同一形式に計測する。各intervalのraw elapsed、実行logic/sound数、現行clipへの到達量、enemy/enemy-bullet slot差分を並べ、実フレーム遅延とcatch-up量の因果を判定する。bounded fixed-step catch-up実装は本診断結果まで保留。

## 変更してよい範囲

- `scripts/verify-display-profile-gearlynx.py`、Makefile診断ターゲット、`evidence/APS-053/`、同README、`ISSUES.md`。
- host側テストを追加する必要がある場合のみ`tests/`。診断ROMを新設しない。

## 触ってはいけない範囲

- release/cadence ROMのC/asm、`src/`、`include/`、`cfg/`、`src/cadence_probe.s`、scheduler、速度セマンティクス、clip値、C stack、背景データ、Suzy collision、ROM内profiler、Phase 3R、`evidence/APS-052/logic-catchup-gearlynx.json`。
- コミット・push・stash・reset・checkout禁止。

## 実装要求

1. 既存公開symbol単独breakpoint方式を使う。新しいターゲット（例:`phase-2r-catchup-causality-gearlynx`）で、fresh/no-reinject両方を4敵NORMALのみ各独立2 batch×10 interval実行する。freshは従来v019どおり毎interval再注入、no-reinjectは従来v021どおり各batch開始前1回だけ注入しinterval再注入0回。既存v019/v021ターゲットの意味を変えない。
2. 各intervalで保存する値: `raw_elapsed_vblanks`、elapsed VBlank、logic counter delta、sound counter delta、現行式から算出したexpected logic=`min(raw_elapsed*4,128)`・expected sound=`min(raw_elapsed,2048)`、`logic_clip_reached`、`sound_clip_reached`、clipで捨てられた量、Timer2、公開display chain、敵/敵弾slot状態と差分。計算値と実カウンタが一致しなければFAIL。
3. 4敵fresh/no-reinjectを、raw elapsed、logic/sound実行数、clip、状態進化、実フレームelapsedの相関で比較する。0敵profile/no-profile陰性対照（絶対中央値差≤1 VBlank、相対差≤5%）も実行する。
4. 判定は次のいずれかを明記すること。
   - `catchup_amplification_supported`: 遅延の大きいintervalでlogic/soundがclip付近または増加し、その後の表示遅延と正相関。
   - `state_cost_dominant`: logic/sound量が実フレーム遅延を説明せず、状態差だけに対応。
   - `mixed_or_inconclusive`: 双方を分離不能。推測でscheduler修理へ進まない。
5. 証跡`evidence/APS-053/catchup-causality-v022.json`に全raw、各batch集計、判定根拠、再現コマンド、ROM SHA/map Segment/MAIN余剰不変を保存する。README/ISSUESへ同じ結論と未解決事項を追記する。

## 完了条件

- 新ターゲットがPASS、または根拠付きFAIL。MCP停止・fixture/readback不整合・対照不成立はFAILとして記録して停止。
- `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、`make debug-contract`、`make static-layer-readback-gearlynx`、対象`py_compile`、`git diff --check`を実行すること。
- release/cadence ROMのC/asm/source/map不変、CODE/RODATA/DATA/BSS/MAIN余剰を数値で報告すること。設計との差分・リスクも報告すること。
