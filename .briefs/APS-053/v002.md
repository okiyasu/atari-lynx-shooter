# APS-053 v002 — 中断partial実装の受入完成

- 課題ID: APS-053
- 版: v002
- 作成日時: 2026-08-12 JST
- 前版: `v001.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 着手前必須

`AGENTS.md`（存在すれば）、`CLAUDE.md`、`ISSUES.md`、`docs/plan/2026-08-11-suzy-sprite-migration.md`、`.briefs/APS-053/v001.md`、APS-051/052証跡を先に読むこと。作業後は`ISSUES.md`を更新すること。コミット・push・stash・reset・checkoutは禁止。

## 前回中断・partial状態

v001担当session `a569625a-c087-40bd-a45d-2da8d123b7c1`はwebsocket idle timeoutで終了。作業ツリーに未検収の`src/static_layer.c`、`src/static_layer_data.c`、`include/static_layer*.h`、`scripts/generate-static-layer.py`、`src/main.c`・Makefile・CFG等の変更が残る。`tgi_clear`/背景/HUDを`SCB_REHV_PAL`と`tgi_sprite()`へ置換したと報告されたが、0敵3 VBlank以下、全3 Stage pixel照合、buffer整合、BSS/stack受入は未達。partialを前提にせずdiffと実機ROM挙動を確認して完成または正確に停止報告すること。

## 変更してよい範囲

v001の許可範囲を継続する。追加で既存`verify-stage-visuals-gearlynx.py`をstatic background/HUDとの合成後もsprite pixelを誤検出しないよう同期/期待値を補正してよい。Phase 3可動object、sprite run/data pipeline、collision・ゲームルール・音声/Timer/IRQ/cartは引き続き変更禁止。`cfg/lynx-voice.cfg`/`cfg/lynx-voice-cadence.cfg`のstack予約変更は禁止。APS-052 evidenceは、同一最新ROMで再測定した場合だけ更新可。古い数値を新ROMの正本として残さない。

## 必須修正・検証

1. static SCBの描画先が毎frame TGIの裏bufferであることをROM readback/連続front-back captureで証明する。`tgi_sprite()`を使う場合は、それがSuzy SCBを投入しTGIのDRAWPAGEを使う根拠とbuffer整合のreadbackをevidenceへ残す。固定address・front buffer描画・swap中競合は禁止。
2. `tgi_clear`、背景、HUDの既存pixel意味を全3 Stage NORMAL/CAST/BOSSで再現し、static layerの独立pixel照合と従来sprite pixel照合をPASSさせる。stage1の`rect=(10,48,8,6) dying=1`同期失敗を原因修正し、fixture/logicを弱めて隠さない。
3. 0敵fixtureを修理済みTimer2 cadenceの独立2 batchで測定し、raw display_request intervalを各3 VBlank以下にする。star layer統合などは許可するがTGI差分描画へ戻さない。達成不能なら実測値・SCB個数・主要ボトルネックを残して停止する。
4. 通常/計測mapのBSS/RODATA/CODE/C stack残余とstack guardをPhase 1比で記録し、BSS/stack安全性を確認する。voice title/GAME OVER、A/C/B、APS-052 4敵logicと4/8/boss+4 sound tickを最新ROMで保全する。

## 完了条件

- `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、関連verifier `py_compile`、`git diff --check` PASS。
- title/GAME OVER、audio channel 0/1/2、`make frame-cadence-gearlynx`を実行。敵0体独立2 batchすべて`<=3 VBlank`、全3 Stage NORMAL/CAST/BOSS pixel照合、TGI/Suzy裏buffer整合、map実測を`evidence/APS-053/`に保存。
- versionは最終ROM作成時に更新。設計差分・実機/長時間IRQ未確認を報告。未達なら成功扱いにせず、次に必要な判断を明記する。
