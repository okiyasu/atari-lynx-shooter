# APS-053 v019 — SOL: 公開symbol表示境界verifier完遂

- 作成日時: 2026-08-12T22:20:00+09:00
- 前版: `v018.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 委譲理由: v018はROM無変更・公開symbol verifierを実装済みだが、Gearlynx MCPが複数公開breakpointの`step_out`再開後に応答停止し、計測値未受入。ユーザー承認によりSOL系担当へ直列切替。

## 着手前に読むもの

存在する`AGENTS.md` / `CLAUDE.md`と`ISSUES.md`を先に読むこと（本プロジェクトは`CLAUDE.md`のみ）。さらに`.briefs/APS-053/v018.md`、`evidence/APS-053/README.md`、`ISSUES.md`先頭、既存表示profile verifier、`src/main.c`、cadence `.lbl/.map`、関連MCP利用スクリプトを読むこと。作業後は`ISSUES.md`を更新すること。

## 目的

公開symbol限定・ROM無変更で、`_static_layer_draw` / `_tgi_ioctl`を含む表示境界診断を安全なMCP手順へ修正して完遂する。0敵NORMAL・4敵NORMAL・4敵+BOSS BOSSを各2 batchで測定し、0敵/4敵の差分と実フレーム遅延の関係を記録する。

## 変更してよい範囲

- `scripts/verify-display-profile-gearlynx.py`（またはv018で追加された同等verifier）、Makefile診断専用ターゲット`phase-2r-display-profile-gearlynx`、`evidence/APS-053/`、同README、`ISSUES.md`。

## 触ってはいけない範囲

- release/cadence ROMのC/asm、`src/`、`include/`、`cfg/`、C stack予約、背景データ、Suzy collision、Phase 3R、ROM内プロファイラ、スケジューラ、閾値、音声/可動object/collision/logic/input/Timer/IRQ。
- 内部関数アドレス推測、`evidence/APS-052/logic-catchup-gearlynx.json`上書き、コミット・push・stash・reset・checkout禁止。

## 必須手順・安全条件

1. v018のMCP停止を再現し、応答停止を避ける手順へ限定修正する。例えばbreakpointを1境界ずつ別プロセス/別runで計測し、`step_out`依存を外せる場合はその方式を採用する。ただし同一frame内の境界差分でないと意味が変わる場合は、結果を未確定として停止する。
2. 公開symbol解決と`tgi_ioctl` ABI分類はv018要件どおり、ソース/cc65 ABI/既存証跡で立証できる場合だけ行う。立証不能なら分類なしの境界時刻記録またはFAIL。推測禁止。
3. 各fixtureでconsume、`_static_layer_draw`、`_tgi_ioctl`（分類可能な`tgi_sprite`/`tgi_busy`/`tgi_updatedisplay`）、display requestの到達/elapsed VBlank/Timer2、fixture readbackを記録する。
4. 0敵profile/no-profile陰性対照: 絶対中央値差≤1 VBlankかつ相対差≤5%。不成立は`debugger_timing_contamination` FAIL。MCPが再び停止した場合はtimeout/最後に成功した境界/再現コマンドを証跡に残し、PASSを偽装しない。
5. release/cadence ROM SHA、map Segment list(CODE/RODATA/DATA/BSS)、MAIN余剰が不変をassert。v018確認値はrelease/cadence spare=`450/264` bytes。

## 完了条件

- `make phase-2r-display-profile-gearlynx`がPASSし、各3 fixture×2 batchの計測・対照・再現コマンドを`evidence/APS-053/display-profile-v019.json`へ保存すること。または環境制約の根拠付きFAIL。
- `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、`make debug-contract`、`make static-layer-readback-gearlynx`、対象`py_compile`、`git diff --check`を実行すること。
- ROM/source/map不変、設計との差分、未解決事項をISSUES/README/報告へ明記すること。
