# APS-053 v024 — Phase 3R到達可能性 tick→VBlank 校正

- 作成日時: 2026-08-13T09:25:00+09:00
- 前版: `v023.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 前回成果: v023 bounded fixed-step catch-up（logic最大12/sound最大4）後、4敵fresh=`38/39`、no-reinject=`36.5/39`、0敵=`22/22` VBlank、判定`state_cost_dominant`。Phase 2R-2の3 VBlank契約は未達。Fable5はPhase3R本実装前にTimer2 tick→VBlank校正と理論下限判定を要求。

## 着手前に読むもの

存在する`AGENTS.md` / `CLAUDE.md`と`ISSUES.md`を先に読むこと（本プロジェクトは`CLAUDE.md`のみ）。`docs/plan/2026-08-12-suzy-sprite-migration-v2.md`、`.briefs/APS-053/v023.md`、`evidence/APS-053/{logic-profile-v016.json,bounded-catchup-v023.json,README.md}`、`scripts/verify-logic-profile-gearlynx.py`、既存Gearlynx cadence verifierを読むこと。作業後は`ISSUES.md`を更新すること。

## 目的

ROM動作コードを変更せず、`scripts/calibrate-cadence-ticks-gearlynx.py`を実装・実行してTimer2 ticks/VBlankを校正する。4敵logic純増`68 ticks`（v016: 0敵18、4敵86）から`logic_min_vblank=68/ticks_per_vblank`を求め、Suzy描画下限を明示した理論下限でPhase3Rの2 VBlank契約到達可能性を判定する。

## 変更してよい範囲

- 新規`scripts/calibrate-cadence-ticks-gearlynx.py`、Makefile、`evidence/APS-053/`、同README、`ISSUES.md`。
- 既存公開symbol、cadence ROM、Gearlynx MCP、Timer2 snapshot/reuseを用いる verifier-only 計測。

## 触ってはいけない範囲

- `src/`、`include/`、`cfg/`、ROM動作、scheduler、Timer/IRQ周波数、GAME_VERSION_STRING、スプライト変換、SCB/Suzy実装、同時出現上限、C stack、背景データ、Phase3R本実装。
- コミット・push・stash・reset・checkout禁止。

## 実装・計測要求

1. Timer2のbackup/currentと公開VBlank counterまたは同等の既存非侵襲境界を用い、複数独立batchでticks/VBlankを計測する。Timer2のwrap、0 VBlank差分、debugger汚染、未校正・不安定値をFAIL扱いにする。
2. 4敵logic純増`68 ticks`の出典をevidenceに固定し、`logic_min_vblank=68/ticks_per_vblank`を算出する。
3. Suzy描画下限は未実装である事実を隠さず、(a)既存測定から安全に導ける下限、または(b)未確定値として残る理由と最小追加計測を区別する。未確定の値をゼロ扱いして2 VBlank PASSにしてはならない。
4. 理論下限判定: 論証済み下限が`>2 VBlank`なら`phase3r_reachability=impossible_before_redesign`。`≤2`でもSuzy描画下限が未確定なら`not_proven_pending_suzy_draw_bound`。logic+描画双方の上限根拠で`≤2`を証明できた場合だけ`reachable_for_gate`。Fable5が指定した「2 VBlank以下ならbpp変換・収支表ゲート、超過なら再設計/撤退」をこの厳密な証拠水準で報告する。
5. 新Makeターゲット（例:`phase-3r-tick-calibration-gearlynx`）でevidence JSONを生成。再現コマンド、ROM/map SHA、MAPセグメント/MAIN余剰、計測数、中央値/分散、判定式、PASS/FAILを保存する。

## 完了条件

- `make phase-3r-tick-calibration-gearlynx`、対象`py_compile`、`make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、`make debug-contract`、`make static-layer-readback-gearlynx`、title/GAME OVER voice、audio channel 0/1/2、`git diff --check`を実行し結果を提示。
- ROM/source不変、設計差分・未確定のSuzy描画下限・Phase3Rへの次ゲートをISSUES/README/報告へ記録。
