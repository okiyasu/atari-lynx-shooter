# APS-053 v015 — Phase 2R-2 cadence section-profile 診断

- 作成日時: 2026-08-12T20:30:00+09:00
- 前版: `v014.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 前回成果: v013でhsize/vsizeとpenpalを修正し、静的pixel readbackはPASS。しかしcadenceは0敵NORMAL `115/117`,`115/117` VBlankで未達。Fable5 v014の再調査は、Phase 1コミット`95a77a9`時点でも0敵≈102.5/115 VBlankであり、描画ではなくcatch-up/loop支配を示した。

## 着手前に読むもの

存在する`AGENTS.md` / `CLAUDE.md`と`ISSUES.md`を先に読むこと（本プロジェクトは`CLAUDE.md`のみ）。さらに`docs/plan/2026-08-12-suzy-sprite-migration-v2.md`、`.briefs/APS-053/v014.md`、`evidence/APS-053/README.md`、`evidence/APS-053/cadence-v-{a,b,c}.json`、`evidence/APS-052/logic-catchup-gearlynx.json`、`src/cadence_probe.s`、cadence verifier群を読むこと。作業後は`ISSUES.md`を更新すること。

## 目的

Fable5 v014 §2の「section-profile」を verifier-only で実装し、cadence支配項を A/B/Cへ帰属する。修理・閾値変更・Phase 3Rは行わない。

## 変更してよい範囲

- cadence Gearlynx verifier（既存`verify-frame-pacing-gearlynx.py`の拡張または専用`verify-section-profile-gearlynx.py`）、Makefileの診断専用ターゲット、`evidence/APS-053/`、`evidence/APS-053/README.md`、`ISSUES.md`、本ブリーフに必要な参照のみ。
- 専用ターゲット名は `phase-2r-section-profile-gearlynx` とする。通常`make verify`にGearlynx依存を追加しない。

## 触ってはいけない範囲

- release/cadence ROMのC/asm、`src/main.c`、`src/game.c`、`src/sound.c`、`src/static_layer.c`、`include/game.h`、`cfg/`、C stack予約、背景データ、Suzy collision、Phase 3R、音声/可動object/collision/logic/input/Timer/IRQ。
- `evidence/APS-052/logic-catchup-gearlynx.json`を上書きしない。Phase 1原本は`git show 95a77a9:evidence/APS-052/logic-catchup-gearlynx.json`で比較し、現行の関連結果はAPS-053の版別証跡へ保存する。
- コミット・push・stash・reset・checkout禁止。

## 実装要求

1. 0敵NORMAL、4敵NORMAL、4敵+boss BOSSの各fixtureを、各10 frame・独立2 batchで計測する。各frameの次の境界で、一度だけbreakpointを張る→hit→即removeする。
   - `_game_sound_tick` 初回hit: section A終了（input/consume/logic）
   - `_game_display_sync_complete`: section B終了（sound/apply/wait）
   - `_game_display_request`: section C終了（draw/display request）
2. 各境界で`_cadence_probe_vblank_count`、`_cadence_probe_logic_update_count`、`_cadence_probe_sound_tick_count`を読み、可能ならTimer 2 currentも保存する。各section差分、全frameとの差分、fixture状態をJSONへ機械可読で保存する。
3. 同一0敵fixtureで「profile breakpointあり/なし」のcadence medianを比較する陰性対照を実施する。乖離なら`debugger_timing_contamination`として本診断をFAIL扱いにし、ROM内プロファイラは実装しない。
4. 注入なし実プレイ（free-run title→開始→STAGE_INTRO→NORMAL、既存controller macroを用いる）を1計測し、fixture人工性を判定する陽性対照にする。既存環境で確立済みの方法がない場合は、未実施の理由と代替根拠をJSON/ISSUESへ記録し、成功と偽装しない。
5. release/cadence ROM SHA、map Segment listとMAIN余剰が**診断前後で不変**であることをassertする。診断がROMを生成・変更する必要があれば停止し、実装せず報告する。
6. `evidence/APS-053/section-profile-v015.json`に生raw・集計・全分岐判定・ROM/map不変結果を残す。どのsectionが60%以上かを記録し、A/B/Cいずれでも根拠なく最適化へ進まない。

## 完了条件

- `make phase-2r-section-profile-gearlynx`が、上記の計測値と対照をJSONへ出力してPASS、または汚染/環境制約を根拠付きでFAILすること。
- `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、`make debug-contract`、対象Pythonの`py_compile`、`git diff --check`を実行して結果を示すこと。
- 既存`make static-layer-readback-gearlynx`を再実行してPASS確認すること。
- 設計差分: v2の「hsize/vsizeが100VBlank悪化の主因」は、Phase 1証跡との比較により撤回候補であることをISSUES/APS-053証跡に明記。ただし設計書v2そのものはFable5/ユーザー承認なしに書き換えない。
- 実行結果、mapのCODE/RODATA/DATA/BSSとMAIN余剰、未解決事項、設計との差分を報告すること。
