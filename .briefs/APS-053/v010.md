# APS-053 v010 — Phase 2R-2 Gate A O5最小chain設計再相談

- 課題ID: APS-053
- 版: v010
- 作成日時: 2026-08-12 18:00 JST
- 前版: `v009.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 担当: Fable5（設計調査のみ。実装・ファイル編集・コミット禁止）

## 必読

`CLAUDE.md`、`ISSUES.md`、`docs/plan/2026-08-12-suzy-sprite-migration-v2.md`、`.briefs/APS-053/v007.md`、`.briefs/APS-053/v009.md`、`evidence/APS-053/phase-2r-gate-a-v009.json`、`evidence/APS-053/README.md`、`src/static_layer.c`、`src/static_layer_data.c`、`include/static_layer_data.h`、`scripts/verify-phase-2r-gate-a-gearlynx.py`、`scripts/verify-static-layer-readback-gearlynx.py`、cc65 Lynx TGI driver sourceを先に読むこと。

## 確定した観測

- O2: releaseのstatic-layer `tgi_ioctl`入口でAX=`0xB36D`がscratch上のSCB headと一致。chain 21件・終端成立。return時は`SCBNEXT=0`、`SPRGO=0`、`SPRSYS=0`、`VIDBAS=0xE018`、`DISPADR=0xC038`。
- O3: `$C038`/`$E018`各8160Bを`0xAA`にしたraw CPU memory観測で、`_tgi_ioctl` return後・`tgi_busy`後とも`$E018`は全byte clear色0へ変化するが、期待static non-clear 243 pixelは0。`$C038`はsentinel不変。
- O4: 旧TGI `tgi_bar`自機描画は同一raw観測で`$C038`に625 ROI差分・94 non-background pixelを作る。Gearlynx raw page/debuggerが主因という仮説は後退。
- 0敵NORMAL cadenceは実GameState 75 sample×2でfixture妥当、full=`116/117`,`115/117` VBlank。Phase 3/性能改善/C stack縮小/背景カートロードは未許可。

## 依頼

次のDev実装用O5診断の設計を具体化すること。O5は**計測ROM専用・最小chain自己試験のみ**で、release source/ROM/BSS/C stack/静的layer本実装を変更しない。

1. cc65 Lynx TGIドライバが受理するSCBの正確なバイトレイアウト・controlビット・offset・penpal・bitmap形式を根拠付きで示す。
2. 1×1または最小サイズの非clear pixel二個を`next`で連結する最小chainの設計値を提示する。第1/第2 pixelそれぞれの期待物理page位置・期待nibble/色、clearとの区別、chain継続判定を含める。
3. O5結果ごとの分岐を決める: (a)両pixel描画、(b)第1のみ、(c)clearのみ、(d)全く変化なし。packed asset/data生成、SCB control/line format、chain traversal、計測手順のどこを次に疑うかを一意に近づける。
4. 静的chainの現行構造に対する最有力差分候補と、O5だけで区別可能かを述べる。推測は根拠と分離して記す。
5. RAM会計・非干渉制約を確認する。診断ROMのmapでMAIN spare≥256B、release SHA不変を必須とする。必要な実装範囲と完了条件を、Devがそのまま実装できる粒度で返す。

## 禁止

実装・ファイル編集・コミット・push・既存未コミット差分への変更、Phase 3着手、3 VBlank閾値緩和、C stack縮小、背景カートロード判断。
