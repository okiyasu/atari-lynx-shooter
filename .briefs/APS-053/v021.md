# APS-053 v021 — 4敵 no-reinject chain 計測

- 作成日時: 2026-08-12T23:20:00+09:00
- 前版: `v020.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 前回成果: v019表示境界診断は公開symbol単独breakpointで完走。Fable5 v020は、4敵153とfree-run32は「毎interval再注入のfresh戦闘状態」と「phaseのみ固定で進化済み4 active敵」の違いで両立すると判定。モデル確定の最小残作業はno-reinject chain 1本。

## 着手前に読むもの

存在する`AGENTS.md` / `CLAUDE.md`と`ISSUES.md`を先に読むこと（本プロジェクトは`CLAUDE.md`のみ）。さらに`.briefs/APS-053/v019.md`、`.briefs/APS-053/v020.md`、`docs/plan/2026-08-12-suzy-sprite-migration-v2.md`、`evidence/APS-053/{display-profile-v019.json,section-profile-v015.json,README.md}`、`scripts/verify-display-profile-gearlynx.py`、`src/cadence_probe.s`を読むこと。作業後は`ISSUES.md`を更新すること。

## 目的

4敵NORMALを初回一度だけ注入して以後のintervalで再注入しないchainを測定し、v019のfresh状態153級からfree-run32級へ遷移するかを証明/反証する。ROM/source/map/スケジューラ/閾値/Phase3Rは不変。

## 変更してよい範囲

- `scripts/verify-display-profile-gearlynx.py`への`--no-reinject`限定追加、Makefile診断ターゲット、`evidence/APS-053/`、同README、`ISSUES.md`。

## 触ってはいけない範囲

- release/cadence ROMのC/asm、`src/`、`include/`、`cfg/`、C stack、背景データ、Suzy collision、ROM内profiler、scheduler、速度セマンティクス、閾値、Phase3R、`evidence/APS-052/logic-catchup-gearlynx.json`。
- コミット・push・stash・reset・checkout禁止。

## 実装要求

1. 4敵NORMALだけを対象にする。各独立batchは開始前に1回だけ既存fixture注入を行い、その後**10 interval中のinject_stateを完全にスキップ**する。`--no-reinject`なし既存v019の動作を変えない。
2. 2独立batchを実行し、各intervalで既存display chainの公開symbol境界、elapsed VBlank、logic/sound counter、Timer2、active敵数に加え、各enemy slotのactive/type/x/y、enemy bullet active数/必要な座標をreadbackし記録する。
3. 0敵profile/no-profile陰性対照を既存条件（絶対中央値差≤1 VBlank、相対差≤5%）で再実行する。4敵no-reinjectでMCP応答停止、fixture readback不整合、対照不成立のいずれかなら根拠付きFAILとして停止する。
4. 証跡`evidence/APS-053/display-profile-v021.json`に、v019 fresh比較値153/154、no-reinject全raw、状態変化、分岐判定、再現コマンド、ROM SHA/map Segment/MAIN余剰不変を保存する。
5. 判定:
   - 153級→32級へ減衰し状態進化を伴う: `state_dependent_model_confirmed` PASS。
   - 状態進化ありでも150級維持: 4敵profile/no-profile陰性対照を追加し、汚染判定後に停止。
   - それ以外: 未確定として停止。最適化/修理/Phase3Rへ進まない。

## 完了条件

- `make phase-2r-display-profile-gearlynx`または明示的なv021専用ターゲットでPASS、または根拠付きFAIL。
- `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、`make debug-contract`、`make static-layer-readback-gearlynx`、対象`py_compile`、`git diff --check`を実行すること。
- ROM/source/map不変、MAPのCODE/RODATA/DATA/BSS/MAIN余剰、設計差分・未解決事項をISSUES/README/報告へ記録すること。
