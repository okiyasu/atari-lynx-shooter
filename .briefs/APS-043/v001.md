# APS-043 / v001

- 作成日時: 2026-08-09 JST
- 前版: なし（APS-042一次検収合格後）
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 事前確認

存在する `AGENTS.md` / `CLAUDE.md`、`ISSUES.md`、`docs/plan/`、`.briefs/APS-041/v001.md`、`.briefs/APS-042/v001.md`、現行`git status --short`を先に読むこと。作業後は`ISSUES.md`を更新すること。APS-041/042の未コミット成果を保全し、commit / push / stash / reset / checkout、BIOS・外部ROM・外部素材の取得・読取・生成・同梱を禁止する。

## 問題・制約

APS-042のvisual canvasはplayer 12x10、通常敵12x12、boss既存寸法へ拡大済みだが、着色cell数はplayer 25/26、scout 20/20、cave_bat 18/18等でスカスカである。最終ROMは60,217 bytes、`sprite_data.o` RODATA 1,245 bytes（282 run）、BSS 1,261 bytes、C stack残余11 bytes。runtime C/assembly/renderer/GameStateは変更禁止。

## 要求

`assets/stages/stages.json`を唯一の正本として、全13 sprite・各2frameを手作業の密度ある1px dot artへ再設計する。単なる幾何プリミティブ、外周だけ、空白で埋めたcanvasは禁止。

- visual/collision canvasはAPS-042のまま（player 12x10 / collision 8x6、normal enemy 12x12 / 8x8、boss 24x16・28x14・24x24 / 同寸法）。`GameRect.x/y`左上anchor、AABB、移動、spawn、発射、難易度、drop、boss config/script、背景、敵弾、音声は不変。
- 各frameで最低3 palette roleを使い、outline、highlight、shadow、機体/生物構造を1px単位で明示する。playerは両frame各50以上の着色cell（APS-042の25/26から明確増）を必須とする。最低cell数は `scout 48`、`saucer 50`、`dropper 50`、`fighter 48`、`bomber 58`、`supply 48`、`cave_bat 48`、`rock_worm 52`、`mining_drone 52`、`coral_bastion 120`、`amber_carrier 135`、`violet_geode 140` とし、各frameに適用する。
- 格納方式は既存fixed horizontal-run RLEのみ。全26frameのrun数はAPS-042と同じ列`7/7,10/10,9/9,12/12,9/9,10/10,10/10,9/9,8/8,8/8,15/15,14/14,20/20`、合計282へ厳密固定する。`sprite_data.o` RODATA 1,245 bytes、LNX size、BSS、MAIN、C stack残余11 bytesを増減させない。
- `docs/plan/`とISSUESに、13種ごとのvisual/collision、各frameの着色cell数、3〜4使用色、silhouette/outline/highlight/shadow/構造意図、2frame差、左scroll時の識別性を表で記録する。

## 変更範囲

変更可: sprite grid、host generator/fixture/golden/test、Gearlynx visual verifier、`evidence/APS-043/`、`include/version.h`、README、`docs/plan/`、ISSUES、`.briefs/APS-043/`。変更禁止: `src/`、`include/game.h`/`sound.h`、Makefile、stage/formation/boss config/environment/palette、runtimeメモリ、collision/AABB、sound/voice/cart/cfg。JSON/hash/parser、展開buffer、動的確保、runtime圧縮解除をROMへ追加しない。

## 完了条件

1. `GAME_VERSION_STRING=0.43.0`へ更新後に最終ROM生成。`tests/golden/sprite-data-v043.json` canonical SHA-256を固定しv042参照を置換。generator/host回帰でID、canvas/collision不変、26frameの正確なrun数282、各frame色数3〜4・cell数下限・canvas内run・frame差・dense offsetを検査する。
2. Gearlynx headless/GUI各連続2回。全13 spriteの個別PNGと9 full-screen PNGを`evidence/APS-043/`へ保存し、authoring grid/hardware paletteとの全非空pixel照合、collision readback、double-buffer同期、headless/GUI hash一致を確認する。
3. `make clean && ./scripts/verify.sh`、ASan/UBSan host全件、`make smoke-host`、`make perf-host`、`git diff --check`、title/GAME OVER/A/C/B Gearlynx回帰を成功させる。LNX/SHA-256、RODATA/BSS/MAIN/stack、cart payload/hash、設計差分・実機未確認を記録する。
