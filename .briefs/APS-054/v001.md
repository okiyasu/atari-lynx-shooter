# APS-054 / v001

- 作成日時: 2026-08-23 00:24:30 +0900
- 前版: なし
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 依頼

実機で「方向入力中の自機移動が飛びすぎる」と報告。append_hud差分更新化の直接回帰か、300Hz logic導入後から潜在していた移動単価の時間基準不整合かを確定し、実装方針を1案に裁定する。実装・編集・commit/pushは行わない。

## 既知の事実

- `38424ef`の動作コード差分は`src/static_layer.c`のみ。`src/game.c`、`include/game.h`、`src/main.c`、入力処理、`GameState.player`更新は変更なし。
- `src/game.c:3`の`PLAYER_SPEED=2u`は初期コミット`c805544`から不変。
- `95a77a9`で実時間catch-upを導入。main loopは入力を1回pollし、`game_logic_updates_for_draw_frame(elapsed_vblanks, 0)`回だけ同じ入力を`game_update_logic()`へ渡す。productionは`elapsed_vblanks * 4`、上限12 logic updates。
- `move_player()`はnormal/warningの各logic updateで、方向入力1回につき2px移動。elapsed=1なら1 outer drawあたり8px、elapsed=3〜4で上限12なら24px。
- append_hud最適化前後のfull fixture: `10.108 VBlank/frame`→`3.127 VBlank/frame`。catch-up上限のため実効logic/VBlankは約`1.19`→`3.84`へ復帰。
- 実機ROMはHUD表示正常、自機移動量過大。`make perf-host`は75 draw/300 logic/75 sound、`game_speed_x=1.00`。現行host testはscheduler回数を検査するが、実機での許容px/sを定義していない。

## 裁定してほしい選択肢

1. 300Hz logicを維持し、playerだけを時間基準化。例: `PLAYER_SPEED=2px/75Hz draw`相当を固定小数/整数accumulatorで4 logic updateに分配し、通常時の表示ジャンプを約2px/drawへ抑制。
2. player移動をlogic update単価のまま維持し、`PLAYER_SPEED`だけを1または別値へ変更。実装は小さいが、入力サンプル間のジャンプ量と目標px/sの整合を要確認。
3. gameplay logic scheduler自体を75Hz相当に戻す、または入力/移動だけ別周期へ分離。300Hz logic・BGM/SFX・catch-up契約との衝突が大きい。

## 制約・検収観点

- append_hudを巻き戻さない。300Hz logic、75Hz sound、catch-upの実時間追従を原則維持。
- player移動だけを直すか、bullet/enemy/scroll等の速度も同じ時間基準で再定義するかを明示。
- 境界(clamp)、方向転換、低FPS/catch-up、入力保持、stage intro/warning、既存host回帰を考慮。
- 目標px/sが未定義のため、合理的な仮定を置く場合は根拠と未確認項目を明示。
