# APS-045 / v001

- 作成日時: 2026-08-10
- 前版: なし（APS-044/v004の承認後続）
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 目的: APS-044で承認済みの自機A案と確認済み敵9種・boss3種の手作業1pxデザインを、既存ゲームの固定visual canvasへ安全に再authoringして反映する。

## 着手前

`AGENTS.md` / `CLAUDE.md` のうち存在するもの、`ISSUES.md`、`docs/plan/`、APS-041〜044のbrief、APS-044 preview JSON/READMEを先に読むこと。`AGENTS.md`は現時点で存在しない。作業前後に`git status --short`を確認し、未コミットのAPS-041〜044成果を保全すること。作業後は`ISSUES.md`を更新すること。コミット・push・stash・reset・checkout禁止。

## 正本と不変条件

- 視覚正本: `assets/previews/aps044-player-preview.json`のA案、および`assets/previews/aps044-enemy-preview.json`の12体。プレビューを概念・配色・部位分離の正本とし、runtimeでは既存canvasへ手作業で再authoringする。縮小画像の自動変換、外部素材、AI生成画像の使用禁止。
- `assets/stages/stages.json`の既存visual canvas/左上anchorを維持: player `12x10`、通常敵9種`12x12`、boss `24x16`/`28x14`/`24x24`。各2frameを意図的に異ならせる。
- collisionは厳守: player `8x6`、通常敵全て`8x8`、boss寸法不変。`include/game.h`、AABB、移動、spawn、発射、難度、stage/boss config/script、state遷移を変更しない。
- title voice後`38 tick`待機、Timer 3/queue/IRQ、BGM開始、GAME OVER復帰、A/C/B音声を維持。動的確保禁止。BSS `0x4ED`=1261 bytes、MAIN最終`0xB82D`、C stack開始`0xB838`、残余11 bytesを悪化させない。
- runtime renderer/palette mappingを変更しない。playerにpreviewの`C` engine色をそのまま持ち込めないため、既存player role `7/8/9`だけでA案のengine/canopy/hull/shadowを表現する。enemy/mineral/bossはそれぞれ既存role集合だけを使う。

## デザイン実装

1. player A: 右向きdelta wing、先細り機首、canopy、下側keel、左engine/nozzleを12x10で明瞭にする。横長帯・塗りつぶし矩形・単なる輪郭を禁止。各frameで密な1pxセル、少なくとも3 role、row span/切欠き/上下非対称を持たせる。
2. normal: `scout,saucer,dropper,fighter,bomber,supply,cave_bat,rock_worm,mining_drone`をAPS-044のwedge/dome/claw/bank wing/armored bay/cargo/bat/segmented drill/asymmetric chassisとして12x12へ再authoring。各機の輪郭、主翼/装甲/砲塔またはノズル、shadow/highlightを既存3 roleで可読にする。
3. bosses: `coral_bastion,amber_carrier,violet_geode`をspires+turret+reactor、bridge+nacelle+engine、facet+nucleus+fissureとして既存canvasへ再authoring。固定3〜4 role、分割された部位、非帯状silhouetteを保つ。
4. 既存の`SPRITE_CONTRACTS` visual/collisionとrun上限を尊重する。先に現行baseline（LNX 60,217 bytes、sprite_data RODATA `0x4DD`=1245 bytes、total runs 282、BSS/stack上記）を記録し、変更後のrun/RODATA/LNX差分を測定する。run合計は282固定を優先し、runtime/header容量を変更しない。これでデザインを達成できない場合は停止して具体的不足を報告する。

## 変更範囲

変更可: `assets/stages/stages.json`の13 sprite frameのみ、必要最小の`tests/test_sprite_data.c`、`tests/test_stage_data.py`、`scripts/generate-stage-data.py`（APS-045 contract/message/golden参照のみ）、`scripts/verify-stage-visuals-gearlynx.py`（APS-045 pixel/capture契約のみ）、`Makefile`の`SPRITE_GOLDEN`参照、`tests/golden/sprite-data-v045.json`、`include/version.h`を`0.45.0`、`README.md`、`ISSUES.md`、`docs/plan/design.md`、`.briefs/APS-045/v001.md`、`evidence/APS-045/`。

変更禁止: `src/`、`include/game.h`、audio/music/cart/cfg/assembly、stage/formation/enemy/boss/environment/palette設定、`assets/previews/`、`evidence/APS-042`〜`044`、既存brief、APS-041のtitle/voice実装、外部ROM/BIOS、ビルド生成物。既存`tests/golden/sprite-data-v043.json`は履歴として残す。

## 検証と証跡

1. sprite canonical v045 golden、全13 ID/canvas/collision/role/two-frame差/各pixel/run/total 282のhost検査、negative mutationを追加または更新する。APS-044 design featureをID別に`docs/plan/design.md`と`ISSUES.md`へ記録する。
2. `make clean && ./scripts/verify.sh`、ASan/UBSan host suite、`make smoke-host`、`make perf-host`、`python3 -m py_compile scripts/generate-stage-data.py scripts/verify-stage-visuals-gearlynx.py`、`git diff --check`を実行する。
3. `scripts/verify-stage-visuals-gearlynx.py --output-dir evidence/APS-045`と`--gui --output-dir evidence/APS-045/gui`を各連続2回実行し、全13個別sprite/Stage 1〜3 NORMAL/CAST/BOSS、hardware palette、pixel readback、GUI/headless SHA-256一致を保存する。actual ROM/LNX path、SHA-256、LNX/RODATA/BSS/MAIN/stack数値をREADME/ISSUESへ残す。
4. title/game-over/A/C/B Gearlynx音声回帰を実行し、38 tick待機とStage 1/BGM開始が不変であることを記録する。

## 完了条件

- A案+12 enemy designsが実ゲーム表示でプレビューの機種識別性を保つ。
- collision/gameplay/state/audio/RAM/stack不変、version `0.45.0`、ROM/LNX生成成功。
- 上記host/smoke/perf/Gearlynx GUI/headless/音声検証が実測PASSし、APS-045証跡を残す。
- 変更、実行コマンドと結果、ROM所在、ROM/RAM差分、設計差分、実機未確認リスクを報告する。
