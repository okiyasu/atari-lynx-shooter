# APS-056 v010

- 課題ID: APS-056
- 版番号: v010
- 作成日時: 2026-08-23 Asia/Tokyo
- 前版: `.briefs/APS-056/v009.md`

## 目的

v009の直接計測を再検証し、敵弾不可視の原因を`GameState→SCB field store`まで確定する。v009 evidenceはGameState slot0=`100,40,1`、SCB slot0=`SPRCTL0=0x28, SPRCTL1=0x08, hpos=0, vpos=0`、slot3=`SPRCTL1=0x0C, hpos=0, vpos=0`を示す。一方、evidence全体statusはPASSだが、`direct_slot0_matches_expected_fixture=false`等をPASS条件へ含めていないため、まず誤PASSを解消する。

## 必須調査

- `SCB_RENONE`のcc65 `_suzy.h` layout（sprctl0=0、sprctl1=1、next=3、data=5、hpos=7、vpos=9、size=11）と、v009の実chain readback解釈を突合。
- `src/main.c:522-535`の`MOVABLE_SET_HPOS/VPOS`、`src/main.c:1135-1157`のenemy-bullet loop、v009 diagnostic traceを確認。cc65生成assembly/objectまたは実行時breakpointでslot0/slot3へのstore先・store値を直接確認。
- `SPRCTL0=0x28`（`BPP_1=0x00`、`TYPE_NONCOLL=0x05`に対する差分）、hpos/vposゼロの発生源を、初期static initializer、別slot書込み、chain pointer誤認、Suzy投入前後のどこかに分類。
- v009のGUI/headless同値が本当に同一原因か確認。framebuffer/screenshotの黒画素は結果証拠として保持し、Suzy/LCD固有原因へ戻さない。

## 検証器修正

- `direct_slot0_matches_expected_fixture`、`direct_slot3_matches_expected_hud_skip`、SCB field expected値をevidence `checks`へ追加し、falseを含む結果を全体PASSにしない。
- active source、SCB SKIP、座標、data/next、投入marker、framebuffer結果を分離表示。未確認値をPASS扱いしない。
- 原因特定に不要な本番変更・対策実装は禁止。必要なら`APS056_DIAGNOSTIC`限定の最小観測だけ追加。

## 変更範囲・完了条件

- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- `CLAUDE.md` / `ISSUES.md`を先に読むこと。作業後は`ISSUES.md`を更新すること。
- 変更可: `scripts/verify-aps056-diagnostic-gearlynx.py`、`tests/test_aps056_diagnostic.c`、`ISSUES.md`、APS-056 evidence、必要最小限の`src/main.c`/`src/game.c`/`include/game.h`。v007〜v009とv008既知変更を保持。
- host/strict/lint、`make clean && ./scripts/verify.sh`、diagnostic LNX、`py_compile`、`git diff --check`をPASS確認。
- 原因をファイル・行・生成コードまたは実SCB証跡付きで報告。開始/終了HEAD、変更ファイル、テスト件数、ROM hash/sizeを記録。
- production ROM挙動・公開API・APS-055以前の記録を変更しない。commit、push、実機投入、stash、reset、checkout禁止。

## 停止条件

仕様矛盾、公開API互換性破壊、既存データ削除・変換、許可範囲超過、認証/本番権限、v007〜v009以外の未確認変更を検出した場合は変更せず報告。
