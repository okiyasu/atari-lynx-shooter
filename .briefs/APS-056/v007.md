# APS-056 v007

- 課題ID: APS-056
- 版番号: v007
- 作成日時: 2026-08-23 Asia/Tokyo
- 前版: `.briefs/APS-056/v006.md`

## 対象

- プロジェクト: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- `AGENTS.md` / `CLAUDE.md` のうち存在するものと、`ISSUES.md` を先に読むこと

## 新しい実機観測

- APS-056診断ROMで、HUDに`E05`のような表示（ユーザー報告: 死因コードと認識）を確認。
- 同じ実機実行で、敵弾はOPT2による色切替を試しても依然として視認できない。
- 現在のhost/diagnostic実装では、`src/static_layer.c`のHUD位置17〜19は、死亡かつ`diagnostic_damage_source != NONE`なら`D0N`（N=1〜4）、それ以外は`E`+敵弾active数2桁を表示する。
- `include/game.h`の死因コード定義は`NONE=0`、`ENEMY_BODY=1`、`ENEMY_BULLET=2`、`ASTEROID=3`、`FALLING_ROCK=4`のみ。`5`は未定義。
- よって`E05`が実際にその3セルなら「敵弾active数5」の可能性があり、死因コード`5`とは限らない。表示の見間違い・HUDセル位置ずれ・実機描画差も含めて判定すること。

## 依頼（解析のみ）

- 上記実機結果と現行コードを照合し、`E05`の正確な意味を断定すること。
- `E05`がactive count=5なら、game logic/spawn/active保持は成立している前提で、敵弾が見えない原因候補を「GameState→SCB値→Suzy/tgi_ioctl→LCD最終画素」の順に絞ること。
- OPT2でpenpal色を切り替えても不可視なことから、色値だけの問題を除外できるか、`SPRCTL0/SPRCTL1`、`data`、`hpos/vpos`、`next`、sprite pixel encoding、SCB chain submission、画面外/クリップ、他レイヤー上書きの各候補をコード根拠付きで評価すること。
- `E05`表示自体が敵弾SCBの存在を証明するか、active countとSCB submissionの間に未計測区間があるかを明記すること。
- 次に実機で一回だけ取得すべき判別情報（例: active count、敵弾SCBのSPRCTL1/hpos/vpos/data/next、penpal、tgi_ioctl投入時刻、最終画素）を、最小変更案として提示すること。

## 変更範囲・禁止事項

- 今回は解析・設計ブリーフのみ。ソース、Makefile、テスト、ISSUES.md、ROM、evidenceの変更禁止。
- commit/push禁止。コード変更が必要と判断した場合は変更せず、必要なファイル・診断項目・理由を報告。
- Fable5への相談禁止。前回裁定（host経路では実機原因を再特定できず、実機判別ROMが必要）を前提にすること。

## 完了条件

- `E05`の意味、敵弾不可視の候補順位、OPT2結果の解釈、次の最小判別計装を含む解析報告。
- コード根拠としてファイルパスと行番号を明記。
- 実機で未取得の値と、hostテストだけでは証明できない点を分離。
- 変更なし、commit/pushなしを確認。

## 停止条件

- 開始時点で`.briefs/APS-056/v007.md`以外の未確認変更、仕様矛盾、既存データ削除・変換、公開API互換性リスク、認証情報・本番権限、許可範囲超過があれば解析を進めず報告。
- 開始時点の既知HEAD: `a0fe46d7b99296667c4e1c20a6fe69d41878b14f`、origin/main同値。v007ブリーフは既知の依頼変更。
