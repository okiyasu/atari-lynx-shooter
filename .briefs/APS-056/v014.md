# APS-056 v014

- 課題ID: APS-056
- 版番号: v014
- 作成日時: 2026-08-23 Asia/Tokyo
- 前版: `.briefs/APS-056/v013.md`

## 再着手理由

v013 Dev sessionは実装前に停止した。`CLAUDE.md`の一般則「commit/push禁止」と、今回のユーザー指示「推奨方針で進め、実装・検証・コミット・pushまで自律的に進める」の優先関係を明示し、開始時点の既知差分を確認済みとして再着手する。

今回のユーザー明示承認はプロジェクト一般則を上書きする。`origin/main`へのcommit/pushを実施すること。これはv013から変わらない既存承認であり、再確認質問不要。

## 既知の開始状態

- HEAD=`ae42873572b1505de4f6c56fc3b644561f879e25`、origin/main同値。
- 作業ツリーの既知差分は`ISSUES.md`（v012診断追記）、`.briefs/APS-056/v012.md`、`.briefs/APS-056/v013.md`のみ。これらはDev Frontが内容確認済みで、未確認変更ではない。
- v014ブリーフ追加後の`?? .briefs/APS-056/v014.md`も本作業の既知変更として扱う。上記以外の変更があれば停止。

## 実装仕様

1. `src/game.c`の敵弾collisionで、`enemy_bullets[i].rect.y < GAME_HUD_HEIGHT`をdamage対象外にする。y=8/9は非damage、y=10以上は既存の2x2半開区間AABBを維持。
2. 通常敵本体collisionの`enemy->rect.x == 0u`短絡damageを削除し、`game_aabb_intersects()`の実矩形判定だけにする。x=0非接触は非damage、接触はdamageを維持。
3. 既存のGameState→SCB描画座標修正、移動順序、damage source、ボスcollision、公開APIを変更しない。

## 作業・検証・成果物

- 先に`CLAUDE.md`と`ISSUES.md`を読むこと。作業後は`ISSUES.md`をv013実装結果へ更新すること。
- host testsへy=8/9/10、非接触敵弾、敵本体x=0非接触/接触、半開境界を追加または既存テストで固定。
- `make aps056-diagnostic-host`、`make test smoke-host lint`、`make clean && ./scripts/verify.sh`、必要なら`make aps056-diagnostic-gearlynx`、`python3 -m py_compile scripts/verify-aps056-diagnostic-gearlynx.py`、`git diff --check`を実行。
- ROM size/SHA-256/LNX/MAIN余剰、テスト件数、開始/終了HEAD、未確認の実機検証をISSUESへ記録。
- テストPASS後、v012/v013/v014ブリーフ、ISSUES、実装、テストを意図した範囲で同一commitに含め、`origin/main`へpush。push後に作業ツリーcleanとHEAD/origin一致を確認。

## 停止条件

- 上記の既知差分以外の変更、仕様矛盾、公開API互換性破壊、データ変換、認証/本番権限、テスト不合格、MAIN余剰不足、範囲外変更がある場合は変更・commit・pushせず報告。
- `CLAUDE.md`の一般的commit/push禁止だけを理由に停止しない。今回のユーザー明示承認が優先する。
