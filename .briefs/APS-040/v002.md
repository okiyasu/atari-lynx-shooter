# APS-040 / v002

- 作成日時: 2026-08-09 JST
- 前版: `v001.md`
- 種別: 一次検収差し戻し（通常敵の個別visual evidence不足）
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 事前確認

存在する `AGENTS.md` / `CLAUDE.md`、`ISSUES.md`、`docs/plan/`、`v001.md`、APS-040現行成果と`git status --short`を先に読むこと。作業後は`ISSUES.md`を更新すること。APS-031〜040の未コミット差分、`stash@{0}`、Git履歴を保全し、commit / push / stash / reset / checkout、BIOS・外部ROM・外部素材の操作を禁止する。

## 差し戻し理由

`evidence/APS-040/`のStage 1〜3 NORMAL/BOSS 6画面はpalette、boss active、double-buffer同期、3 bossの表示を確認できる。しかしNORMAL画面にはinitial enemyが画面外に残るため、APS-040で詳細化した9種通常敵（scout/saucer/dropper/fighter/bomber/supply/cave_bat/rock_worm/mining_drone）が個別にrenderされた証跡になっていない。fixed grid/host回帰だけでなく、Gearlynx framebuffer上で全対象の輪郭・配色・識別性を確認する必要がある。

## 要求

`scripts/verify-stage-visuals-gearlynx.py`をhost検証器として最小拡張し、production runtime・sprite data・versionを変更せず、各stage NORMAL上の通常敵3種を同時に画面内へ表示してcaptureする。

- Stage 1: SCOUT(0) / SAUCER(1) / DROPPER(2)、Stage 2: FIGHTER(3) / BOMBER(4) / SUPPLY(5)、Stage 3: CAVE_BAT(6) / ROCK_WORM(7) / MINING_DRONE(8)を、既存final ROMのpaused `GameState.enemies[4]`へdebug memory injectionする。敵弾・power item・gameplay dataを生成しない。
- `GameEnemy`のcc65実レイアウトを`include/game.h`とmap/既存scriptのoffsetから検算する。各active enemyは8x8、type、既存patternに矛盾しない最小field、互いとplayer/HUDに重ならないscreen内rect（例: x=40/80/120、y=24/47/70）を設定する。注入後にtarget type/active/rectをmemory readbackし、描画/表示swapを既存のbreakpoint同期で1回以上完了してからcaptureする。推測のoffsetで書かない。
- `stage1-cast.png`、`stage2-cast.png`、`stage3-cast.png`を`evidence/APS-040/`へ保存する。各captureはstage palette一致、3 active enemy/type/rect readback、通常player表示、3種のそれぞれ異なるrun/color dataを使用することを検査し、失敗時はstage/type/rectを含む短いdiagnosticで終了する。
- `--gui`対応では同じ3 cast画面を`evidence/APS-040/gui/`にも作成し、headless/GUIで対応PNG SHA-256が一致することを検査する。READMEへ既存6画面と追加3画面のhash、何を表示する証跡か、state injectionはrender検証であってゲームプレイ代替ではないことを追記する。
- 実機表示ではなくGearlynx rendered framebufferの確認である点は維持する。GUIが利用不能なら事実を報告し、GUI成功と偽らない。
- 変更可: visual verifier、`evidence/APS-040/`、README/design/ISSUES、`.briefs/APS-040/`、host-only test補助のみ。変更禁止: `assets/stages/stages.json`、golden、`tests/test_sprite_data.c`、runtime `src/`、`include/game.h`、`include/version.h`（`0.40.0`を維持）、sound/voice/cart/cfg/Makefile、ゲームstate・敵弾・当たり判定・難易度。

## 完了条件

1. `python3 scripts/verify-stage-visuals-gearlynx.py --output-dir evidence/APS-040`と`--gui --output-dir evidence/APS-040/gui`が、6既存場面+3 cast場面を連続2回成功すること。9通常敵のtype/rect/readbackと9 PNG hashを報告すること。
2. `make clean && ./scripts/verify.sh`、ASan/UBSan付きhost全件、`make smoke-host`、`make perf-host`、`git diff --check`、title/GAME OVER/A/C/B Gearlynx回帰を成功させること。
3. LNX SHA-256・size、`GAME_VERSION_STRING=0.40.0`、BSS/RAM、sprite RODATA、cart payload/hashがv001最終ROMから不変であることを確認すること。設計差分・実機未確認を構造化報告すること。
