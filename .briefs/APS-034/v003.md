# APS-034 / v003

- 作成日時: 2026-08-09 JST
- 前版: `v002.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 差し戻し理由

v002実装のhost/ROM検証は通過したが、Dev Frontが最終ROMで独立再実行した`python3 scripts/verify-stage-visuals-gearlynx.py`が以下で失敗した。固定`time.sleep()`後のphase期待がheadless実行速度に依存しているため、Gearlynx表示検証は合格と認められない。

```
FAIL: stage 1 did not enter active BOSS: state=b'\\x01\\x02\\xac\\x00' active=b'\\x00'
```

## 作業

`AGENTS.md` / `CLAUDE.md`の存在するもの、`ISSUES.md`、`v001.md`/`v002.md`、現行成果を先に読むこと。既存APS-031〜034差分を保全し、commit/push/stash/reset/checkout禁止。作業後は`ISSUES.md`を更新すること。

`scripts/verify-stage-visuals-gearlynx.py`を、hostの固定sleep時間でNORMAL/BOSSを判定しない決定的な方式へ直す。Gearlynx MCPが提供するframe/step/run制御があれば使い、なければpause状態での状態注入と描画を適切に同期する。正規遷移を確認するなら、目標phase/boss activeを短間隔pollし、通過して取り逃さない状態設計にする。最終手段のstate注入は、何を検証でき何を検証できないかを正確に記録する。少なくとも最終ROMでStage 1〜3のNORMAL/BOSS各画面、生成palette 32 byte一致、sprite/color描画、BOSS active、PNG evidence 6枚を安定して確認する。

既存host/generator/sprite/game/ROM実装をリファクタリングしない。必要なら検証scriptと関連README/design/ISSUES/evidenceだけを変更してよい。`GAME_VERSION_STRING`は既にユーザー指定の`0.35.0`であり変更しない。JSON正本、生成C、sprite/palette、ADPCM/BGM/SFX、ゲーム速度に触れない。

完了条件: 少なくとも連続2回の`python3 scripts/verify-stage-visuals-gearlynx.py`成功（各runの出力・対象ROM SHAを報告）、`make clean && ./scripts/verify.sh`、LNX、`git diff --check`成功。実機・GUI連続playthrough未確認は維持する。設計差分を構造化報告すること。
