# APS-044 / v004

- 作成日時: 2026-08-10
- 前版: `v003.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 変更理由: v003でplayer generatorを修正すると、そのscript自身のSHA-256が変化する。一方sheet generatorはplayer generator source hashを固定検証するため、v003の変更範囲では両`--check`を成立させられない。

## 着手前

`AGENTS.md` / `CLAUDE.md` の存在するもの、`ISSUES.md`、`docs/plan/`、`v001.md`〜`v003.md`、本ブリーフを先に読むこと。作業前後に `git status --short` を確認し、未コミットのAPS-041〜044成果を保全すること。作業後は `ISSUES.md` を更新すること。コミット・push・stash・reset・checkout禁止。

## 許可する最小修正

1. `scripts/generate-aps044-player-preview.py`: `evidence/APS-044/`に存在する他所有者の3 sheetを許容し、自身の明示8 player PNGだけを検証対象にする。8 PNGの全pixel/SHA-256/独立再生成byte一致を維持する。
2. `scripts/generate-aps044-character-sheets.py`: 上記player generatorの新しいsource SHA-256を固定値として更新する**1箇所だけ**。現在の旧固定値 `ffb3bd1a891af31f3c1db00d00c3287aa1952527d932aee1d130e086f26b8846` は変更される。player JSONと8 PNG、enemy JSON、3 sheetのcontent/SHA-256は変更禁止。

README/ISSUES/designには、v004の理由、両script source SHA-256、全11 PNGの不変値を記録する。JSON/grid/PNGを再生成して内容が変わった場合は停止し、上書きせず報告する。

## 変更範囲

変更可: `scripts/generate-aps044-player-preview.py`、`scripts/generate-aps044-character-sheets.py`のplayer source SHA-256固定値1箇所、必要最小限の`evidence/APS-044/README.md`、`ISSUES.md`、`docs/plan/design.md`、`.briefs/APS-044/v004.md`。

変更禁止: `assets/previews/`の両正本、既存11 PNG、`assets/stages/stages.json`、`src/`、`include/`、Makefile、tests/golden、runtime/ROM/LNX/version、他のAPS成果。

## 完了条件

1. `python3 scripts/generate-aps044-player-preview.py --check && python3 scripts/generate-aps044-character-sheets.py --check` が同一evidence directoryで連続PASS。
2. `python3 -m py_compile scripts/generate-aps044-player-preview.py scripts/generate-aps044-character-sheets.py`、stage generator validate、`git diff --check`がPASS。
3. 8 player PNGと3 sheet PNGのSHA-256がv002記録値と一致し、全11 artifact名集合・pixel・byteが不変。
4. `ISSUES.md`に両scriptの新SHA-256と依存関係を記録し、設計差分を報告。

