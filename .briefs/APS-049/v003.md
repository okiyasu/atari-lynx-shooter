# APS-049 v003

- 作成日時: 2026-08-10T00:00:00+0900
- 前版: `.briefs/APS-049/v002.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 優先度: 高

## 前版からの経緯（再度セッション断絶による再委譲）

v002を実行したセッションは契約g（cadence実時間計測）の手法切り分けを完了し、旧手法（毎フレームMCPブレークポイント往復）が計測アーティファクトの原因であることを確定させた（校正テストで往復頻度依存の膨張なしを確認、round-trip inflation比1.000）。低頻度方式で0/4/8 normal・boss+4normalを再測定した結果、全fixtureで実測59〜119us、13,333us予算に対し**1%未満**という潤沢な余裕が判明した。ここまでは正しく完了・報告された。

その後Ryokoが2点の指示（低頻度方式の`verify-frame-pacing-gearlynx.py`本体への正式統合、boss 2x実測ゲート付き採用）を追送したところ、セッションがフォールバック起動の設定不整合（Codex専用`--ephemeral`フラグがClaude Code退避時に混入）で**送信内容が処理されないままクラッシュ**した。これは前セッション（`.briefs/APS-049/v002.md`作成の原因）と同一のバグで、2回連続再現している。ゲートウェイ側のインフラ不具合であり本プロジェクト側では対処不能なため、以後dev従業員への追送(`/message`)は行わず、必ず新規セッション・自己完結ブリーフで継続する。

**作業ツリーは無傷**（commit/push/stash/reset/checkoutは一切行われていない）。`git status --short`で46ファイルの変更が確認でき、`scripts/calibrate-cadence-ticks-gearlynx.py`と`scripts/verify-frame-pacing-lowfreq-gearlynx.py`が新規プロトタイプとして存在する。

着手前に必ず`ISSUES.md`の「APS-049」節と`git status --short`を読み、現在の作業ツリー状態を正確に把握すること。前セッションの成果を上書き・破棄しない。

## 着手前

`AGENTS.md` / `CLAUDE.md` のうち存在するもの、`ISSUES.md`（特にAPS-049節）、`docs/plan/design.md`、`.briefs/APS-048/review-v001.md`（Fable5独立レビュー）、`.briefs/APS-049/v001.md`（元ブリーフ、要件詳細）、`.briefs/APS-049/v002.md`（前版）、本v003を読むこと。commit/push/stash/reset/checkout禁止。

## 完了済み（そのまま維持、再実装しない）

- B節(sprite単一ソース化)、契約a/b/c/d/e、RAM残余389 bytes、boss visual scale暫定1x固定 — v002完了分。
- 契約g手法切り分け: `scripts/calibrate-cadence-ticks-gearlynx.py`（校正テスト、ticks/nop≈9.06線形・round-trip非依存を確認）、`scripts/verify-frame-pacing-lowfreq-gearlynx.py`（低頻度再測定プロトタイプ、全fixture PASS）、`evidence/APS-049/cadence-tick-calibration.json`、`evidence/APS-049/frame-cadence-lowfreq-gearlynx.json`。

## Ryokoによる承認事項（今回初めて反映される指示）

契約gの結論を承認する。診断は正しかった（旧手法のブレークポイント往復オーバーヘッドが計測に混入していた、実性能バグではない）。以下2点を実施すること。

1. **低頻度方式の正式統合**: `verify-frame-pacing-gearlynx.py`本体へ低頻度方式を契約gの正本として統合する。既存の移動量デルタ等のregressionチェック（旧方式・毎フレームinjection）はそのまま維持し、cadence assertion部分だけ低頻度方式に差し替える二本立てにする。統合後、`scripts/verify-frame-pacing-lowfreq-gearlynx.py`は重複を避けるため`verify-frame-pacing-gearlynx.py`に一本化し、プロトタイプのまま分離して残さない。
2. **boss 2x scale最終判定**: 契約gがボトルネックでないことが判明したため、基本方針（2x採用）へ進めてよい。ただし机上判断ではなく、**boss 2x描画を実際に有効化した状態で低頻度方式による契約g再測定を必ず行い**、13.3ms予算内（かつ現状のような潤沢な余裕があるか）を確認してから最終決定すること。予算内であれば2xを正式採用、超過するならv001記載のfallback（1x等倍+collision中心整列）へ切り替え、その判断根拠（実測値）を証跡に残す。

Timer2 VBLANK IRQと表示要求の周期比1:3の不一致（v002セッションが検出、合否には無関係と記録済み）は今回の合否判定対象外。解明不要、証跡への記載のみで足りる。

## 未完了・本セッションで着手する内容

上記1・2の反映に加え、v002ブリーフに記載していた残作業を継続する:

- 契約f（capture pixel、preview起点でのframebuffer照合）の実装。
- Gearlynx証跡13種（per-sprite native capture、GUI拡大、stage cast、実プレイ画面）一式を`evidence/APS-049/`へ保存（v001記載の構成に従う）。
- boss 2x有効化・`verify-frame-pacing-gearlynx.py`統合など、フルビルドに影響する変更を行った時点で`make verify`を必ず再実行すること（v002セッションでは製品コード無変更のため未実行だったが、本セッションでは境界を超えるため必須）。

## 変更してよい範囲 / 触ってはいけない範囲 / 完了条件

v001の該当節をそのまま適用する。追加で、`scripts/verify-frame-pacing-gearlynx.py`・`scripts/verify-frame-pacing-lowfreq-gearlynx.py`・`scripts/calibrate-cadence-ticks-gearlynx.py`の統合・整理も「変更してよい範囲」に含まれる。

## 完了条件（v001から改めて明記）

- `GAME_VERSION_STRING`を`0.49.0`へ更新し、clean ROMを再生成。
- `make clean && ./scripts/verify.sh`、host test suites、`make smoke-host`、`make perf-host`、stage/sprite negative tests、`python3 -m py_compile`、`git diff --check`が成功。
- 契約a〜g全緑（gは統合後の`verify-frame-pacing-gearlynx.py`で実行）。0/4/8 normal・boss+4normalの1 draw frame実時間計測値を証跡に記載。
- 13種判別可能なGearlynx capture一式（native hash正本+GUI拡大+実プレイ画面）を`evidence/APS-049/`へ保存。
- RAM残余 ≥ 11 bytesをmap実測で報告。collision・当たり判定・開始音声後38 tick待機・音声系・重み付き容量契約が不変であることをdiffで示す。
- boss 2x採用可否の判定結果（採用/fallback）とその根拠（実測値）を報告。
- ブリーフとの差分、実機未確認事項、ROM絶対パス/size/SHA-256を報告。commit/pushしない。

## 報告

進捗はSTATUS/CHANGED/TESTS/DIFF/RISK/NEXTで簡潔に報告すること。commit/push禁止は継続。
