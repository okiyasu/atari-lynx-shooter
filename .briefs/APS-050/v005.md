# APS-050 v005

- 作成日時: 2026-08-10T00:00:00+0900
- 前版: `.briefs/APS-050/v004.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`
- 優先度: 高

## 前版からの経緯(4回連続クラッシュ、委譲先をdev-53へ切替)

v001〜v004の4セッションすべてが実コード変更前にcodexインフラ側エラーでクラッシュした(v001: context window超過、v002: stream disconnected、v003/v004: rate-limit解除後の自動再開が`thread/resume`失敗)。`bin/codex-active-worker`を確認したところ出力が`dev-53`(Codex主枠が90%以上、spark枠は90%未満)に変わっていた。`dev`(主枠луна)がrate-limitへ繰り返し当たっていた原因は主枠逼迫そのものであり、`--ephemeral`運用のセッションがrate-limit中に停止すると自動再開が`thread/resume`(非永続セッションには存在しないrollout参照)で失敗する構造的な組み合わせ不整合と併発していたと判断する。本v005は**委譲先を`dev-53`(spark枠、gpt-5.3-codex-spark)へ切り替えて再委譲**する。`git status --short`で確認した限り**作業ツリーはAPS-049 v003完了時点のまま無傷**(変更ファイル数46、boss scale関連の実装差分なし)。

着手前に同様の確認を行い、想定外の変更が入っていないことを確かめること。

## 本体要件

v001〜v004の内容をそのまま実施する。以下、全文を再掲する。

---

## 背景

APS-049でboss 3種(coral_bastion, amber_carrier, violet_geode)にdraw時2x scale(`src/main.c`の`draw_sprite_run_scaled`、`SPRITE_CONTRACTS`の`boss_visual_scale=2`)を採用したが、実プレイ確認でcoral_bastion(stage1)とviolet_geode(stage3)がboss停止位置(stop_x)で画面右端160pxを約3px(32px中約9%)クリップする不具合が判明した(amber_carrierはクリップなし)。既存の契約a〜gはこの種の画面端超過を検出できていなかった。

ユーザー承認(2026-08-10): 選択肢(b)「全3ボスを1x等倍+当たり判定中心整列に統一、表示の一貫性を優先」を採用する。

## 着手前に読むこと

`AGENTS.md`/`CLAUDE.md`のうち存在するもの、`ISSUES.md`(APS-049節)、`docs/plan/design.md`、`.briefs/APS-048/review-v001.md`、`.briefs/APS-049/v001.md`〜`v003.md`、本v005。commit/push/stash/reset/checkout禁止。

## 変更対象

1. `scripts/generate-stage-data.py`: `SPRITE_CONTRACTS`の`coral_bastion`/`amber_carrier`/`violet_geode`の4項目目(boss_visual_scale)を`2`→`1`に変更。58〜64行付近の2x採用コメントを実態に合わせて修正。`sprite_anchor()`はscale引数込みで中心整列を再計算する設計のため、コード変更は不要 — 生成し直せば新anchorが自動算出される。
2. `src/main.c`: `draw_sprite()`内、788〜789行の`origin.scale = (sprite_id >= GAME_SPRITE_CORAL_BASTION && ... ) ? 2u : 1u;`分岐は、変更後は全sprite scaleが1になり意味を持たなくなる。分岐を削除し`origin.scale = 1u`固定に簡略化すること(不要な複雑さを残さない)。728〜739行のコメント(2x/32x32-equivalent記述)を実態に合わせて修正。`draw_sprite_run_scaled`自体を残すか元の非scaled描画に戻すかは実装判断でよいが、デッドコードや矛盾したコメントを残さないこと。
3. `scripts/verify-stage-visuals-gearlynx.py`: 8〜11行目docstringと905〜906行付近のコメント(「1x normal/player, 2x boss」)を全sprite 1xの実態に修正。ロジックは`SPRITE_CONTRACTS`のscale値を汎用参照しているため動作変更不要のはずだが、要確認。
4. `docs/plan/design.md`・`ISSUES.md`: 2x関連記述の更新、APS-050節の新設(不具合内容・原因・修正内容・差分)。
5. 生成物(`assets/stages/*`, ROM側sprite table等)は`generate-stage-data.py`再実行で再生成。新golden `tests/golden/sprite-data-v050.json`を追加(既存v043/045/047/049は履歴として並存させる既存慣習に倣う)。

## 新規contract(回帰防止)

今回の不具合は既存契約では検出できなかった。全3ボスについて、ステージ内でboss到達しうる全stop_x(少なくとも最大到達位置)においてvisual bboxが画面0〜160px範囲内に収まることを検証する自動契約を追加すること(既存contract fへの統合でも新設(例: h)でも実装判断に委ねる)。

## 不変条件

当たり判定(全collision rect寸法・位置)、ゲームルール・移動・難度・状態遷移、開始音声後の待機・音声系、動的確保なし・スタック残バイトを侵食しないこと(APS-049時点389 bytes残)。1px A案自機・敵9種・ボス3種のピクセルデザイン自体(色・形状)は変更しない — scale/anchorのみが変更対象。

## 完了条件

1. 全3ボスがscale=1xで描画され、既存anchor機構によりcollision中心とvisual中心が整列。
2. 各ボスの到達しうる停止位置で画面端(0〜160px)を一切クリップしないことを自動契約で証明。
3. `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host` 全PASS。
4. `scripts/verify-stage-visuals-gearlynx.py --check`(`--gui`含む)PASS。boss3種の実プレイスクリーンショットを`evidence/APS-050/`に新規保存 — stop_x付近まで実際に到達させた状態で撮影し、画面端クリップがないことを目視でも確認できる証跡にすること。
5. `include/version.h`の`GAME_VERSION_STRING`を`"0.50.0"`へ更新。
6. `git diff --check` PASS。
7. `ISSUES.md`にAPS-050節を追加し、設計との差分を記載。
8. commit/push禁止。

## 報告フォーマット

既存の圧縮フォーマット(STATUS/CHANGED/TESTS/DIFF/RISK/NEXT)に従うこと。該当しないキーは省略可(DIFF: noneのように書かない)。
