# APS-052 v004 — gateway中断後の再委譲: Phase 1音声実時間/guard完了

- 課題ID: APS-052
- 版: v004
- 作成日時: 2026-08-11 JST
- 前版: `v003.md`
- 対象: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 着手前必須

`AGENTS.md`（存在すれば）、`CLAUDE.md`、`ISSUES.md`、`docs/plan/2026-08-11-suzy-sprite-migration.md`、`.briefs/APS-052/v001.md`〜`v003.md`、本ブリーフを先に読むこと。作業後は`ISSUES.md`を更新すること。コミット・push・stash・reset・checkoutは禁止。

## 前回中断・現状

v003担当session `1cc9b190-cdf2-4fa1-bcdc-00d027779ec5`はgateway restartで`Interrupted: gateway shutting down`となり完了通知なし。作業ツリーには未検収の部分変更がある。stack guardの式は一部修正済みだが、`src/main.c`は`game_timing_consume_vblanks()`の16-bit返値を`unsigned char elapsed_vblanks`へ縮小しており、sound tick safety cap `GAME_SOUND_TICKS_MAX=24`も定義だけで未使用。既存のpartial変更を前提にせず、差分を検査して完成させること。

## 目的

BSS/CFG/stack予約を一切変更せず、正しいdownward cc65 stack余力判定と、低FPS中も実時間75Hzへ追従するA/C/B音声を完成させる。前回の`stack_end-lowest_sp`は使用量で、未使用量は`lowest_sp-stack_start`である。

## 変更してよい範囲

- `src/main.c`、`src/game.c`、`include/game.h`、必要最小のsound timing APIとhost回帰
- `src/game_timing.s`、`scripts/verify-frame-pacing-gearlynx.py`、`scripts/verify-title-voice-gearlynx.py`、`scripts/verify-audio-gearlynx.py`
- `ISSUES.md`、`README.md`、`docs/plan/design.md`、`evidence/APS-052/`、`include/version.h`

触ってはいけない範囲:

- BSS量、`cfg/lynx-voice.cfg`、`cfg/lynx-voice-cadence.cfg`、C stack予約、voice/BGM/SFX asset、cart構造
- Phase 2〜4のSuzy/TGI描画、sprite/stage/衝突/難度/入力ルール、外部依存/BIOS/外部ROM・素材

## 実装条件

1. normal/cadenceのstack guardは`stack_start <= lowest_sp <= stack_end`かつ`lowest_sp - stack_start >= 128`をPASSとする。出力/evidenceにstack start/end、min sp、used=`stack_end-lowest_sp`、free=`lowest_sp-stack_start`を残し、v002の87/79 bytesは使用量だったと訂正する。
2. main loopはTimer 2 elapsedを**16-bitのまま**受ける。logicは既存128 update clip、soundは`game_sound_ticks_for_draw_frame(elapsed_vblanks)`による明示的24 tick clipを使用し、`game_sound_tick()`は実elapsed VBlankごと最大24回、`sound_backend_apply_all()`はdrawごと1回だけ実行する。sound discardがあり得る条件はテスト可能・記録可能にする。300Hz logic更新数でsoundをtickしない。
3. title/GAME OVER blocking voice中のA/C/B停止、Timer 3/DAC排他、終了後baseline resetを保全する。
4. channel 0の8秒pitch変化不足とchannel 1のgain mismatchを、production経路または計測競合のどちらかに再現可能に切り分けて修正する。測定競合ならsound-apply完了と論理outputの同一世代を確実に対応付けるverifierへ直す。音量変換`floor(logical*3/4)`・非zero最低1、音楽/SFXデータ、pitch/duration/priority/envelopeの変更と閾値緩和は禁止。
5. APS-052実logic計測（4敵ratio `0.95..1.05`、clip=0）、通常/計測ROM分離、0/8/boss+4のclip/discard記録を維持。描画cadence gの期待FAILを隠さない。

## 完了条件

- `make clean && ./scripts/verify.sh`、`make smoke-host`、`make perf-host`、`python3 -m py_compile scripts/verify-frame-pacing-gearlynx.py scripts/verify-title-voice-gearlynx.py scripts/verify-audio-gearlynx.py`、`git diff --check`成功。
- `make frame-cadence-gearlynx`は描画gの期待FAIL。ただし4敵logic条件・normal/cadence stack guard PASSをJSONで確認。
- `python3 scripts/verify-title-voice-gearlynx.py --mode title`、`python3 scripts/verify-title-voice-gearlynx.py --mode game-over`、`python3 scripts/verify-audio-gearlynx.py --channel 0`、`python3 scripts/verify-audio-gearlynx.py --channel 1 --seconds 8`、`python3 scripts/verify-audio-gearlynx.py --channel 2 --seconds 20`を実行してPASS。
- `GAME_VERSION_STRING=0.52.3`、normal/cadence map/stack/ROM SHA-256/evidence、設計差分・未確認の実機4/8敵体感/長時間IRQを記録。
