# APS-029 実装ブリーフ v002

- 課題ID: APS-029
- 版: v002
- 作成日時: 2026-08-08 JST
- 前版: `v001.md`
- 対象プロジェクト: `/Users/mammycloud-m4/Documents/develop-m4/atari-lynx-shooter/`

## 差し戻し理由

v001の範囲外変更を検出した。着手前のmain (`b1236ad`) はクリーンであり、次の差分はAPS-029として許可しない。全て**基点の内容へ戻す**こと。

- `assets/music/stage1_bass.mml`、`assets/music/stage2_bass.mml`、`assets/music/stage3_bass.mml`の曲データ、音量、duration、休符の変更。
- `include/sound.h`、`src/sound.c`、`src/game.c`の`sound_stop_bgm()`追加および死亡時BGM停止/再出撃時再開始の挙動変更。
- `tests/test_sound.c`のベースライン音楽データ/挙動に関するテスト変更。
- `tests/test_game.c`の死亡時BGM停止/再開始を期待する変更。

上記はv001の「触ってはいけない範囲」(BGM/SFXデータ・duration・テンポ・freeze/stop規則、個別速度値)に明確に反する。ユーザーはBGMテンポを本課題のスコープ外と指定した。`SOUND_TEST_DISABLE_BASS=1`の一時A/Bという説明も、最終ツリーにその定義・参照が存在せず再現できないため、ISSUES.mdから削除すること。

## 残すべきAPS-029の変更

- `include/game.h`: `GAME_LOGIC_UPDATES_NUMERATOR 2u`、`GAME_LOGIC_UPDATES_DENOMINATOR 1u`。
- `tests/test_game.c`: schedulerテストのみを`2/1`仕様へ更新。既存の死亡/BGM挙動のテスト期待は基点へ戻す。
- `README.md`、`docs/plan/design.md`: 75Hz描画/入力/音tickを維持し、ロジック150Hz・基準75Hz比2.00倍とする記述整合。
- `include/version.h`: `GAME_VERSION_STRING`はユーザー指定とv001の完了条件どおり、必ず`"0.29.0"`。現在の`"0.30.0"`は誤り。
- `ISSUES.md`: APS-029の実装実績を、実際に残る変更・実際に再実行した検証結果だけへ訂正。状態は検証完了(コミット・pushなし)。
- `.briefs/APS-029/v001.md`/`v002.md`: 追跡用に残す。

## 必須検証

1. 最終`git diff --name-only b1236ad --`を確認し、上記「残すべきAPS-029の変更」以外が無いことを報告する。生成物は除外してよい。
2. **最終状態のまま**`make clean && ./scripts/verify.sh`を実行する。一時的なソース変更、テスト無効化、A/Bは行わない。
3. **最終状態のまま**既存のASan/UBSan手順、`make smoke-host`、`make perf-host`、`git diff --check`を実行する。
4. `make rom`、`./scripts/inspect-lnx.sh`、`shasum -a 256 dist/asteroid-patrol.lnx`を最終状態で実行する。
5. Gearlynxの未検証事項は正確に記載する。コミット・push・stash操作は禁止。

最終報告は実行コマンドと実測値、残った変更ファイル、ROM SHA-256、設計差分だけを構造化形式で返すこと。
